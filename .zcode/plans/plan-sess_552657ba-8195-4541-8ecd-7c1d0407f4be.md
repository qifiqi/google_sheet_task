基于只读审计，建议按三阶段实现，不提交 commit、不修改 tests/archive、不做破坏性历史 JSON/数据库迁移。

### 已确认问题
- `app/services/performance_analysis/metrics.py:1577-1579` 读取不存在的 `index_df/start_df['return_range']`，外层 `except` 在 `:1766-1770` 吞错并返回空结果。
- `annual_rate_of_return()` 在 `:527` 计算但未进入最终结果。
- 回撤在 `metrics.py:54-60、115-121、150-162` 是正跌幅并取 `idxmax`；mapper、多品和 Word 又重复取负。
- 新结果仍生成 `sotino_ratio`、`excess_sharp`、`excess_of_promissory_note`；mapper 固定 flat 字段并把缺失值转 0。
- 多品服务已有 `:399-448、523-565` 的日收益复利/legacy helper，但 Word `strategy_backtest_report_service.py:160-214` 另有一套组合器；Word payload `backtest_multi_product_service.py:1515-1522` 缺少 `weighting_mode`。
- 多品预览使用固定 `SUMMARY_ROW_DEFS` (`backtest_multi_product_service.py:40-60`)，单品 fallback 和 Word 也只展示固定字段/重新派生日月收益。
- C3/C4/C5/C7 调 `get_return_analysis_v1`，训练单品调 `get_calculate_metrics_v1`；保存层已把收益序列写入 `TaskResultReturn` 并从结果/日志清理大数组，应做兼容收敛而非搬移历史数据。

### 阶段一：P0 正确性
1. 修复日度分布来源，改为 `daily_return` 并保留 bucket/count/percentage；缩小异常捕获，数据不足使用 null，不再无条件返回 `{}`。
2. 统一标准负值回撤 `nav / running_peak - 1`、年度/总计最小值；新增公共 `calculate_drawdown_advantage(strategy, benchmark) = strategy - benchmark`，所有出口只读取 canonical 值。
3. 正式返回年度收益/年化收益字段，清除死计算。
4. 新生成结果使用 `sortino_ratio`、`excess_sharpe`、`excess_sortino`、`excess_nav = 1 + (start_return-index_return)`；读取历史时兼容 typo、`excess_net` 和旧正值回撤，且只转换一次。
5. 新增 `app/services/performance_analysis/portfolio_combiner.py`，统一比例解析（30/30%/0.3 -> 0.30）、共同日期交集、空输入、`daily_compound` 默认算法（累计 -> 日收益 -> 比例加权 -> 复利累计）和显式 `legacy_cumulative`（累计收益直接加权）。多品服务 helper 改为薄封装，Word 停用独立组合公式。
6. 增加 P0 单元/集成测试：非空 V1、bucket、负回撤、excess_nav、Sortino 兼容、回撤优势、两种组合模式、比例和日期边界。

### 阶段二：P1 统一入口与展示协议
1. 新增统一 facade `calculate_v1_metrics(returns, *, runtime_params=None, return_dataframes=False)`，内部唯一调用 `_calculate_metrics_v1`；扩展 `response_dto.py` 增加带 `schema_version` 的 `MetricsV1Result`，包含 canonical metrics、三条 NAV/收益 DataFrame、日/月序列、drawdown、distribution、charts_data 及纯 JSON 投影。
2. 将 `get_calculate_metrics_v1`、`get_calculate_metrics_v1_with_dataframes` 改为 facade 薄封装；`get_return_analysis_v1` 改为 `facade -> LegacyMetricsAdapter`，保留旧 flat API 但不再把它当完整指标。
3. C3/C4/C5/C7 继续保留各自 Sheet/K 线/版本/百分比适配，适配后的 canonical returns 一律进入 facade；backtest_training 同样进入 facade；保留旧结果键读取兼容，TaskResult 只存摘要/canonical metrics，TaskResultReturn 存原始累计序列，TaskLog 只存日志。
4. 多品结果明确分为 `product_metrics`、`weighted_product_metrics`、`portfolio_metrics`；旧 `calculate_metrics/weighted_calculate_metrics` 仅读取兼容。修正多品 detail 和运行时图表的产品/组合语义。
5. 新增共享 `metric_registry.py`，集中管理 canonical key、显示名、分组、格式/null、适用范围、preview/Word/export 可见性和 legacy alias。多品 `SUMMARY_ROW_DEFS`、单品 fallback、Word、CSV/XLSX、detail/API 均从注册表投影，新增 V1 指标不再静默丢失。

### 阶段三：报告复用与 P2
1. `StrategyBacktestReportRequestDTO` 增加 `weighting_mode` 并兼容旧 boolean；多品 Word payload 显式携带 mode。Word 单品/多品均由 facade 生成结果，报告层只编排/格式化，直接消费 canonical 日/月/分布/回撤/charts_data，不重复实现同名公式或取负。
2. 全局预览、Word、Excel/XLSX 共用组合器、三类 metrics 和 registry；缓存键加入 mode、比例、收益序列 hash、日期策略和 metrics version；验证同输入下 preview/Word return 与 metrics 在 daily_compound/legacy_cumulative 均一致。
3. 在报告请求内引入 `ReportContext` 或等价缓存，确保组合器和 facade 各一次，日/月/分布/图表只生成一次，并用 spy/mock 覆盖。
4. 先在 canonical result/metadata 设计 `return_series_hash`、`metrics_version`、`weighting_mode`、`common_date_policy`，不新增必需列；保留现有 Task/TaskResult/TaskResultReturn/TaskLog 职责及历史读取兼容。

### 验证顺序
先运行现有相关 unit/integration 基线，再按阶段运行定向测试，最后运行完整 `pytest`；只取消确已修复的 skip。最终报告列出修改文件、数据流、指标出口、兼容策略、legacy weighting、测试通过/失败/跳过/未覆盖、历史风险和未实施 P2。