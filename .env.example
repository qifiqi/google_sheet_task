# Common configuration
APP_ENV=development
SECRET_KEY=your_secure_random_key_here_at_least_32_characters_long
MAX_CONCURRENT_TASKS=5
TASK_TIMEOUT=3600
LOG_LEVEL=INFO
BASE_URL=http://localhost:5000
PUBLIC_BASE_URL=
DING_TALK_DETAIL_BASE_URL=
SQLALCHEMY_ECHO=False
FLASK_DEBUG=False
DATABASE_URL=postgresql://validator_user:validator_password@127.0.0.1:5432/googlesheet_validator

# Remote stock data API (required when using SDK-backed CRUD endpoints)
# STOCK_BASE_URL 同时用于单 Token 身份校验（SysUser/GetUserInfo 等）。
STOCK_BASE_URL=http://stock-api.example.internal:8081
STOCK_API_TOKEN=
STOCK_API_TIMEOUT=10

# Auth（单 Token 子服务模式，2026-09 启用）
# 本服务不再签发/存储账号；调用方在请求头 Token 字段携带主 Web 登录
# 颁发的单一 Token，服务端经 STOCK_BASE_URL 的 GetUserInfo 校验，
# 路由表经 GetUserRoleList 获取（见 app/routes/meta_api.py）。
# 以下为已停用的本地登录/RBAC 配置，注释保留便于恢复:
AUTH_ENABLED=false
JWT_SECRET_KEY=your_jwt_secret_key_here
# Keep false until the main Web supplies user-scoped model_code values.
REMOTE_MODEL_ACCESS_ENFORCED=false
REMOTE_MODEL_CODES_CLAIM=model_codes
# 临时使用本地 User/RBAC/导航表；后续设为 true 即可重新启用主 Web sys_user/sys_model 网关。
REMOTE_IDENTITY_GATEWAY_ENABLED=false

DING_TALK_ACCESS_TOKEN=your_ding_talk_access_token
DING_TALK_SECRET=your_ding_talk_secret

# DingTalk stream microservice
DING_STREAM_CLIENT_ID=your_ding_stream_client_id
DING_STREAM_CLIENT_SECRET=your_ding_stream_client_secret
