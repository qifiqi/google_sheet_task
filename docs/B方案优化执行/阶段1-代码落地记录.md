# 阶段1-代码落地记录

## 本次目标

将统一 `config schema` 从文档落到代码入口，先控制“任务创建”和“任务配置更新”两条写入链路，再开始拆分 `TaskManager` 的读侧职责。

## 已完成

### 1. 新增统一配置模块

新增文件：

- `app/services/config_schema.py`

当前已实现：

- `normalize_task_config(config, task_type)`
- `validate_task_config(config, task_type)`

当前策略：

- `google_sheet_C5` 作为统一目标结构
- `google_sheet_C4` 按 C5 思路做兼容映射
- 默认 `google_sheet` 暂时保留现状，仅做最小校验

### 2. 接入任务创建入口

涉及文件：

- `app/routes/api.py`
- `app/services/task_manager.py`

处理方式：

- API 创建任务前先归一化，再校验
- `TaskManager.create_task()` 再做一次兜底归一化和校验

结果：

- 前端即使传入半旧结构，也会先被收敛后再落库
- 后续新入口接入时，不容易绕过统一契约

### 3. 接入任务配置更新入口

涉及文件：

- `app/routes/api.py`
- `app/services/task_manager.py`

处理方式：

- 详情页编辑提交的数据在 API 层先归一化，再校验
- `TaskManager.update_task_config()` 再做一次兜底处理

结果：

- 详情页编辑不再轻易把旧结构直接写回数据库
- 配置会逐步向统一结构收敛

### 4. 抽离 TaskManager 查询侧

涉及文件：

- `app/services/task_query_service.py`
- `app/services/task_manager.py`

处理方式：

- 新增 `TaskQueryService`，集中承接以下只读能力：
  - `get_task_status`
  - `get_all_tasks`
  - `get_task_logs`
  - `get_task_results`
  - `check_local_task_status`
- `TaskManager` 对外方法名和返回结构保持不变，内部统一委托到 `task_query_service`
- 查询服务不直接依赖运行态对象，由 `TaskManager` 传入 `running_tasks`、`get_task_logs`、`_get_config`，先保证兼容

结果：

- `TaskManager` 开始从“大而全”收敛到“任务编排与生命周期控制”
- 查询类 API 后续可以复用同一层查询服务
- 为下一步继续拆写侧、状态恢复和仓储层留出边界

### 5. 清理未接入的 RESTX 路由文件

涉及文件：

- `app/routes/api_restx.py`

处理方式：

- 检查 `create_app()` 的蓝图注册流程
- 检查项目内是否存在 `api_restx.py` 的导入、注册或间接引用
- 确认该文件当前未接入运行链路后，直接移除，避免与现有 `app/routes/api.py` 形成重复维护

结果：

- 统一保留当前真实生效的 Blueprint API 实现
- 消除一份未接线但内容重复的历史接口文件
- 后续接口优化只需要围绕 `app/routes/api.py` 和对应服务层继续推进

### 6. 收口前端配置归一化逻辑

涉及文件：

- `templates/google_sheet/base.html`
- `templates/google_sheet_c5/create.html`
- `templates/google_sheet_c5/detail.html`

处理方式：

- 在基类模板中新增共享函数 `normalizeTaskConfigForC5()` 与 `parseTaskConfigObject()`
- 创建页改为直接复用共享归一化入口，不再单独维护一份 `normalizeC5Config`
- 详情页读取和编辑弹窗回填也改为使用同一套解析规则，保证创建页与详情页的配置兼容行为一致

结果：

- C5 页面层的旧结构兼容逻辑从“多处散落”收敛为“单点维护”
- 后续如果 schema 再增加字段，只需要优先维护共享解析函数
- 降低创建页和详情页出现字段默认值不一致的风险

### 7. 补齐模板链路的统一 schema

涉及文件：

- `app/routes/api.py`
- `templates/google_sheet_c5/create.html`

处理方式：

- 模板列表按 `task_type` 过滤时，先对模板配置做统一归一化，避免旧模板因历史字段结构不同被漏掉
- 模板创建、模板更新接口接入 `normalize_task_config()` 和 `validate_task_config()`
- 模板详情返回前先做一次归一化，前端回填时直接使用统一结构
- 修正创建页模板列表加载时的返回值判断逻辑，避免前端错误放过异常响应

结果：

- 模板保存、模板回填、任务创建三条链路现在共享同一套 schema
- C5 模板即使是历史旧结构，也更容易被正确识别和回填
- 降低模板筛选、模板加载、创建页回填之间的结构漂移风险

### 8. 开始拆分 TaskManager 写侧持久化职责

涉及文件：

- `app/services/task_repository.py`
- `app/services/task_manager.py`

处理方式：

- 新增 `TaskRepository`，集中承接任务创建、重启任务复制、任务配置写回、历史结果清理、级联删除等数据库持久化动作
- `TaskManager` 继续保留流程编排、状态控制、线程管理和业务约束校验
- 先抽离低风险的持久化方法，避免一次性把运行态逻辑和数据库逻辑同时搬动

结果：

- `TaskManager` 进一步从“既管流程又管落库”收敛到“以编排为主”
- 后续如果继续拆 `TaskStatusService` 或 `TaskRuntimeRegistry`，边界会更清晰
- 写侧重构已经进入 B 方案阶段 2，而不是停留在只拆读侧

### 9. 抽离运行态注册表

涉及文件：

- `app/services/task_runtime_registry.py`
- `app/services/task_manager.py`
- `app/routes/api.py`

处理方式：

- 新增 `TaskRuntimeRegistry`，集中管理任务线程和事件队列
- `TaskManager` 改为通过注册表创建、登记、清理线程与事件队列
- API 的 SSE 与确认事件入口也改为优先通过注册表读取运行态资源

结果：

- `TaskManager` 不再直接承担运行态资源容器的主要维护职责
- 线程注册、事件队列注册、SSE 读取这几类操作开始有独立边界
- 阶段 2 已从“拆读侧 + 拆写侧”继续推进到“拆运行态”

### 10. 开始抽离任务状态判断服务

涉及文件：

- `app/services/task_status_service.py`
- `app/services/task_manager.py`

处理方式：

- 新增 `TaskStatusService`，集中处理取消任务、重启任务、配置编辑前的状态校验
- 将重启原因生成、断点重启计划生成从 `TaskManager` 中抽离
- `TaskManager` 改为组合查询服务、仓储层、运行态注册表和状态服务，进一步收敛到编排职责

结果：

- 取消、重启、配置编辑这几类典型状态判断开始具备独立边界
- `TaskManager.restart_task()` 中的分支判断明显收缩，后续更适合继续补自动化测试
- 阶段 2 已继续向“状态流转逻辑拆分”推进

### 11. 收口执行器重复收尾逻辑

涉及文件：

- `app/services/task_manager.py`

处理方式：

- 抽出 `_finalize_task_execution()`，统一处理执行结果对应的状态回写与日志记录
- 抽出 `_mark_task_error()`，统一处理执行器异常时的错误状态落库
- 抽出 `_cleanup_task_runtime()`，统一处理线程登记与事件队列清理

结果：

- 三套执行器的尾部重复逻辑明显减少
- 后续如果继续合并 `google_sheet` / `C4` / `C5` 执行链路，收尾阶段已有共享基础
- `TaskManager` 内部重复分支进一步下降，便于后续补状态流转测试

### 12. 合并三套 Google Sheet 执行外壳

涉及文件：

- `app/services/base_google_sheet_service.py`
- `app/services/google_sheet_service.py`
- `app/services/google_sheet_service_C4.py`
- `app/services/google_sheet_service_C5.py`

处理方式：

- 新增 `BaseGoogleSheetService`，统一承接配置解析、任务取消检查、通知发送、公共日志、SSE 推送、工作表列表查询等基础能力
- 将三套 `execute_task()` 外壳收敛到基类，子类只保留 `_run_task()` 与各自参数生成、Sheet 初始化、结果保存差异
- 保留 `C4` / `C5` 的 Advisory Lock 行为和默认版的单模板分支判断，避免合并时改变既有业务分支

结果：

- 三套 Google Sheet 服务已共用同一条任务执行主链路
- 后续继续合并参数生成、结果解析与结果保存时，不再需要重复维护外层执行壳
- “合并三套 Google Sheet 执行链路”这一步已经完成到可继续验收的状态

### 13. 继续上提 Service 公共基础方法

涉及文件：

- `app/services/base_google_sheet_service.py`
- `app/services/google_sheet_service.py`
- `app/services/google_sheet_service_C4.py`
- `app/services/google_sheet_service_C5.py`

处理方式：

- 将基础结果保存 `_save_task_result()` 上提到 `BaseGoogleSheetService`
- 删除默认版与 C4 中重复的日志格式化、数据库日志保存、SSE 推送、工作表查询等公共方法
- 保留 C5 的结果保存扩展逻辑，仅让其继续处理 `TaskResultReturn` 的差异数据

结果：

- 三套 Service 的公共基础能力继续从“外壳共用”推进到“内部通用方法共用”
- 默认版与 C4 的重复代码量再次下降，后续更容易继续抽离多工作表初始化能力
- 代码审查时可以更聚焦各版本真正的业务差异，而不是重复基础设施代码

### 14. 统一单表/多表初始化入口

涉及文件：

- `app/services/base_google_sheet_service.py`
- `app/services/google_sheet_service.py`
- `app/services/google_sheet_service_C4.py`
- `app/services/google_sheet_service_C5.py`

处理方式：

- 在基类新增 `_init_single_google_sheet()` 与 `_init_multi_google_sheets()`
- 默认版改为复用单表初始化辅助方法
- `C4` / `C5` 改为复用多表初始化辅助方法，保留各自后续业务逻辑不变

结果：

- Google Sheet 连接初始化入口开始统一到基类
- 默认版、C4、C5 的连接参数校验与日志输出规则保持一致
- 后续如果继续治理连接重试、代理、认证文件策略，只需要优先维护基类

### 15. 收口 C5 结果保存扩展逻辑

涉及文件：

- `app/services/base_google_sheet_service.py`
- `app/services/google_sheet_service_C5.py`

处理方式：

- 基类继续承担基础 `TaskResult` 落库逻辑
- `C5` 的 `_save_task_result()` 改为先复用基类保存基础结果，再补充 `TaskResultReturn` 扩展收益曲线数据
- 将 C5 的结果保存职责从“整段重写”收缩为“基础复用 + 差异补充”

结果：

- C5 结果保存与默认版/C4 的公共主链路进一步统一
- `TaskResultReturn` 仍保留在 C5 独有差异层，不会影响其他版本
- 后续如果继续整理结果存储模型，差异入口已经更集中

### 16. 统一 C4/C5 轮询等待节奏

涉及文件：

- `app/services/base_google_sheet_service.py`
- `app/services/google_sheet_service_C4.py`
- `app/services/google_sheet_service_C5.py`

处理方式：

- 在基类新增轮询等待辅助方法 `_build_poll_delay_getter()` 与 `_sleep_before_next_poll()`
- 将 C4/C5 中相同的退避等待逻辑统一改为复用基类
- 保持两套结果检查与刷新参数逻辑不变，仅统一等待时间生成与日志输出

结果：

- C4 与 C5 的轮询等待节奏开始共用同一套策略
- 后续如果要统一轮询次数、超时策略、刷新间隔，只需优先修改基类
- 结果轮询骨架进一步具备上提条件

### 17. 抽离 C4/C5 多表轮询骨架

涉及文件：

- `app/services/base_google_sheet_service.py`
- `app/services/google_sheet_service_C4.py`
- `app/services/google_sheet_service_C5.py`

处理方式：

- 在基类新增 `_poll_multi_sheet_results()`，统一承接多工作表轮询的外层 for 循环、完成统计、超时返回
- `C4` / `C5` 改为只提供 `poll_single_sheet` 回调，保留各自结果解析与差异数据组装
- `C4` 继续保留重写参数后的刷新逻辑，`C5` 继续保留轮询前刷新参数逻辑，但都挂到公共轮询骨架上

结果：

- C4/C5 多表执行链路从“共享等待节奏”继续推进到“共享轮询外骨架”
- 后续如果继续治理轮询次数、完成判断、重试刷新策略，改动入口已经更集中
- 三套 Google Sheet 服务的公共骨架进一步清晰，真正的版本差异继续收缩到局部回调

## 本次验证

已执行：

```bash
python -m py_compile app/services/task_runtime_registry.py app/services/task_repository.py app/services/task_query_service.py app/services/task_manager.py app/routes/api.py app/services/config_schema.py
python -m py_compile app/services/task_status_service.py
python -m py_compile app/services/task_manager.py
python -m py_compile app/services/base_google_sheet_service.py app/services/google_sheet_service.py app/services/google_sheet_service_C4.py app/services/google_sheet_service_C5.py
```

结果：

- 通过

## 当前状态

阶段 1 已完成的核心内容：

- 统一配置 schema 已进入创建和更新入口
- C5 详情页已修复新建跳转、编辑弹窗配置回填和保存保留问题
- `TaskManager` 的主要读侧方法已抽离到查询服务

仍未完成的部分：

- 统一前端侧 `normalizeTaskConfig`
- 逐步收缩 `TaskManager` 中已不再需要的旧读侧实现
- 为 `config_schema` 增加自动化测试

## 下一步

建议继续按 B 方案推进：

1. 清理 `TaskManager` 中已被委托替代的旧查询实现，减少重复维护成本。
2. 为共享前端配置解析补充页面级联调验证，重点覆盖重启任务、模板回填、详情编辑三条链路。
3. 开始拆分写侧能力，评估是否需要引入 `TaskRepository` 或 `TaskStatusService`。

### 18. 合并 C4/C5 多表批处理主链路

涉及文件：
- `app/services/base_multi_sheet_google_sheet_service.py`
- `app/services/google_sheet_service_C4.py`
- `app/services/google_sheet_service_C5.py`

处理方式：
- 新增 `BaseMultiSheetGoogleSheetService`，统一承接 C4/C5 共用的多表执行主循环、任务取消检查、输入区清理、多表初始化、模板参数接口与结果保存前主流程。
- `C4` 改为只保留参数计划生成、单项执行逻辑和结果解释逻辑，通过 `_build_batch_plan()`、`_execute_batch_item()`、`_get_input_columns()` 接入共享骨架。
- `C5` 同样切到新基类，并通过运行时上下文缓存同源 K 线组合，仅保留参数计划、参数刷新差异、收益曲线扩展保存等版本专属逻辑。

结果：
- 阶段 3 从“共享执行外壳”进一步推进到“共享多表批处理主链路”，C4/C5 的重复代码量显著下降。
- 当前三套 Google Sheet 实现里，多表版本的差异已经基本收敛到参数生成、结果解释和 C5 的扩展保存。
- 后续如果继续推进“合并 Google Sheet 三套实现”，优先切入点已经从主循环转为结果模型与参数策略层。

### 19. 合并默认单表批处理骨架

涉及文件：
- `app/services/base_single_sheet_google_sheet_service.py`
- `app/services/google_sheet_service.py`

处理方式：
- 新增 `BaseSingleSheetGoogleSheetService`，统一承接默认单表版本的批处理主循环、断点恢复、单表初始化、模板参数接口与执行成功后的扩展钩子。
- 默认 `google_sheet_service` 改为只保留任务入口策略、单项执行、结果校验与参数组合索引计算，原先重复的批处理外层流程全部收口到单表基类。
- 将默认版“执行成功后回写股票模板参数”的动作收敛到 `_after_item_success()`，让保存结果与扩展动作的边界更清晰。

结果：
- 三套 Google Sheet 实现现在都已经具备清晰的“基类骨架 + 版本差异钩子”结构。
- 默认版与 C4/C5 在任务执行组织方式上进一步对齐，后续继续合并时可以更聚焦在结果模型、参数策略和校验规则。
- 阶段 3 的“合并 Google Sheet 三套实现”已从执行链路层面基本打通，进入更细的规则统一阶段。

### 20. 统一结果解析与收益曲线组装规则

涉及文件：
- `app/utils/google_sheet_result_helper.py`
- `app/services/google_sheet_service.py`
- `app/services/google_sheet_service_C4.py`
- `app/services/google_sheet_service_C5.py`

处理方式：
- 新增 `google_sheet_result_helper`，统一承接结果值解析、结果区是否刷新判断、无效标记判断与收益曲线序列构造。
- 默认单表版本改为复用统一的单值解析逻辑，避免继续在 Service 内部重复维护百分比、逗号、异常标记等转换规则。
- C4/C5 改为共用结果区变化判断和收益曲线组装规则，减少多表版本内部的重复结果解释代码。

结果：
- 三套实现开始共享同一层“结果解释规则”，后续继续统一校验策略时改动入口更集中。
- 结果解析逻辑从 Service 业务代码中抽离出来，便于单独补测试和排查边界值问题。
- 阶段 3 已进一步推进到“执行骨架统一 + 结果解释规则收口”的状态。

### 21. 启动阶段 4 的 API 路由拆分

涉及文件：
- `app/routes/api.py`
- `app/routes/api_task_routes.py`
- `app/routes/api_config_routes.py`
- `app/routes/api_template_routes.py`
- `app/routes/api_google_sheet_routes.py`
- `app/routes/api_result_routes.py`
- `app/routes/api_log_routes.py`
- `app/routes/api_database_routes.py`

处理方式：
- 将原本单文件的 `api.py` 拆为按职责分组的多个路由模块，分别承接任务、配置、模板、Google Sheet 辅助、结果、日志、数据库运维等接口。
- `api.py` 收敛为蓝图装配入口，只负责创建 `api_bp` 并注册各个子模块，避免继续在一个文件中堆积数百行路由实现。
- 保持对外 URL 不变，优先做“内部结构拆分”，降低后续继续拆前端模板时的耦合风险。

结果：
- 阶段 4 已正式开始，API 层已经从“单文件巨型入口”切到“按职责分模块注册”的结构。
- 后续继续拆模板与页面脚本时，可以按相同边界对齐接口，不需要再先处理 API 单文件膨胀问题。
- 当前这一步不改接口协议，只改代码组织，风险相对可控，适合作为阶段 4 的起点。

### 22. 抽离创建页公共保存模板弹窗

涉及文件：
- `templates/google_sheet/_save_template_modal.html`
- `templates/google_sheet/create.html`
- `templates/google_sheet_c4/create.html`
- `templates/google_sheet_c5/create.html`

处理方式：
- 新增 `google_sheet/_save_template_modal.html`，把三套创建页完全一致的“保存为模板”弹窗提取为可复用模板片段。
- 默认版、`C4`、`C5` 三个创建页不再内联维护相同弹窗结构，统一改为 `{% include "google_sheet/_save_template_modal.html" %}`。
- 在 include 位置补充中文注释，明确该弹窗已经进入共享模板层，便于后续继续拆 `taskStatusModal` 与公共脚本。

结果：
- 阶段 4 已从 API 层拆分推进到模板层拆分，三套创建页开始真正共享同一份 HTML 结构。
- 后续如果调整保存模板字段或按钮文案，只需要修改一个模板片段，降低三处页面同步修改遗漏的风险。
- 这一步属于低风险重复代码收口，为继续拆状态弹窗和模板脚本打下了稳定入口。

### 23. 抽离创建页公共任务状态弹窗

涉及文件：
- `templates/google_sheet/_task_status_modal.html`
- `templates/google_sheet/create.html`
- `templates/google_sheet_c4/create.html`
- `templates/google_sheet_c5/create.html`

处理方式：
- 新增 `google_sheet/_task_status_modal.html`，统一承接创建页任务执行进度、状态日志与详情跳转按钮结构。
- 三套创建页只保留 `task_detail_url` 差异，通过页面内设置变量后复用同一份状态弹窗模板。
- 在模板中补充中文注释，说明公共弹窗依赖页面传入详情页地址，方便后续继续统一状态轮询脚本。

结果：
- 三套创建页的两个核心弹窗都已经进入共享模板层，模板重复代码进一步下降。
- 详情页地址差异被收敛为单一变量，不再需要复制整块状态弹窗 HTML。
- 阶段 4 的模板拆分已经从“单个弹窗复用”推进到“按差异参数复用”的层次，为下一步抽公共脚本提供了稳定边界。

### 24. 抽离创建页公共模板弹窗脚本

涉及文件：
- `templates/google_sheet/_template_modal_helpers.html`
- `templates/google_sheet/create.html`
- `templates/google_sheet_c4/create.html`
- `templates/google_sheet_c5/create.html`

处理方式：
- 新增 `google_sheet/_template_modal_helpers.html`，统一承接 `saveAsTemplate()` 与 `submitTemplate()` 两段模板弹窗公共脚本。
- 三套创建页不再各自维护相同的模板保存逻辑，只保留 `getCurrentConfig()`、`loadTemplates()` 等页面专属差异实现。
- 在公共脚本入口补充中文注释，明确页面级依赖是“配置采集”和“模板列表刷新”，避免后续继续拆分时边界再次混乱。

结果：
- 创建页模板保存链路已经从“公共弹窗 HTML”继续推进到“公共弹窗脚本”，重复代码进一步下降。
- 三套页面现在共享模板弹窗结构、状态弹窗结构和模板保存动作，后续更适合继续抽模板列表加载与本地缓存逻辑。
- 阶段 4 已经形成“模板片段 + 公共脚本 + 页面差异钩子”的稳定拆分模式，可以继续进入更细的前端整理。

### 25. 将创建页与详情页内联脚本外移到独立 JS

涉及文件：
- `templates/google_sheet/create.html`
- `templates/google_sheet/detail.html`
- `templates/google_sheet_c4/create.html`
- `templates/google_sheet_c4/detail.html`
- `templates/google_sheet_c5/create.html`
- `templates/google_sheet_c5/detail.html`
- `static/js/google_sheet/create.js`
- `static/js/google_sheet/detail.js`
- `static/js/google_sheet/create_c4.js`
- `static/js/google_sheet/detail_c4.js`
- `static/js/google_sheet/create_c5.js`
- `static/js/google_sheet/detail_c5.js`
- `static/js/google_sheet/template_modal_helpers.js`

处理方式：
- 将三套创建页和三套详情页原本内联在模板中的大段脚本全部抽离到 `static/js/google_sheet/` 目录下的独立 JS 文件。
- 模板层只保留最小化的脚本引用，让 `create.html` / `detail.html` 回到“结构负责视图、脚本负责行为”的职责边界。
- 原先模板里的公共模板弹窗脚本同步转移到 `static/js/google_sheet/template_modal_helpers.js`，不再继续保留模板脚本片段版本。
- 在脚本抽离完成后，对 6 个页面模板执行 Jinja 解析校验，并对 7 个 JS 文件执行 `node --check` 静态语法校验。

结果：
- 阶段 4 的“把超大模板里的脚本抽到独立 JS”已经落地，`create.html` / `detail.html` 内联 JS 体积显著下降。
- 页面模板的改动冲突面明显收窄，后续继续修改任务创建、详情编辑、模板回填时，不需要再在上千行 HTML 模板中找脚本。
- 三套 Google Sheet 页面现在已经具备“模板负责结构、JS 文件负责行为”的基础形态，阶段 4 的目标已完成。

### 26. 启动阶段 5 的 pytest 标准化测试骨架

涉及文件：
- `pytest.ini`
- `tests/conftest.py`
- `tests/api/test_task_routes.py`

处理方式：
- 新增 `pytest.ini`，把项目测试入口统一到 `tests/` 目录，避免继续依赖零散的 `test/` 脚本手工执行。
- 新增 `tests/conftest.py`，统一创建隔离的 Flask 测试应用与 SQLite 测试数据库，为接口级回归测试提供稳定夹具。
- 新增 `tests/api/test_task_routes.py`，先覆盖任务详情、配置更新、任务重启、创建重启任务四条关键接口链路。
- 在配置更新用例中显式校验 `C4` 配置会先归一化成统一 schema，再交给 `TaskManager`，保证阶段 1 到阶段 4 的结构收敛成果能被测试守住。
- 由于项目历史模型存在 SQLite 下的重名索引问题，测试夹具暂时采用“按需建表”的方式，只创建当前用例必需的表，先保证阶段 5 能稳定起步。

结果：
- 阶段 5 已正式开始，项目从“有零散脚本”进入“有标准 pytest 入口”的状态。
- 任务详情、配置更新、重启相关接口已经拥有第一批自动化回归样例，后续可以继续补任务状态流转和 Google Sheet 依赖 mock。
- 下一步可以继续把现有 `test/` 目录中真正有价值的脚本迁移到 `tests/`，逐步收拢为标准测试体系。

### 27. 重组历史 test 目录，明确新旧测试边界

涉及文件：
- `tests/README.md`
- `tests_legacy/README.md`
- `tests_legacy/manual_scripts/`

处理方式：
- 将历史 `test/` 目录整体迁移到 `tests_legacy/manual_scripts/`，统一作为遗留脚本归档区保留。
- 保留 `tests/` 作为新的标准 `pytest` 测试目录，不再让历史脚本与自动化测试混放。
- 新增目录说明文档，明确 `tests/` 只承载标准自动化测试，`tests_legacy/` 只承载旧脚本与迁移参考。

结果：
- 项目测试目录职责已经分清，后续不会再出现“自动化测试”和“临时脚本”混杂在同一个 `test/` 目录里的情况。
- 旧脚本仍然完整保留，方便后续按功能逐步迁移，而不会影响当前 `pytest` 收集规则。
- 阶段 5 的测试体系布局已经从“补几条测试”扩展到“建立清晰目录结构”，适合继续补充更完整的回归用例。

### 28. 补充任务创建与本地状态检查接口回归测试

涉及文件：
- `tests/api/test_task_routes.py`
- `tests/conftest.py`

处理方式：
- 为 `POST /api/tasks` 补充创建任务成功链路测试，显式校验 `google_sheet_C5` 配置在创建前会先归一化为统一 schema。
- 为 `POST /api/tasks` 补充缺少 `config` 的参数校验测试，确保接口继续返回 400。
- 为 `GET /api/tasks/<task_id>/status-check` 补充本地状态检查测试，验证路由层会直接透传 `TaskManager` 返回值。
- 在测试夹具中补充 `curl_cffi` 与 `yfinance` 的轻量 stub，避免 API 层测试在收集阶段被外部行情依赖阻塞。

结果：
- `tests/api/test_task_routes.py` 当前已覆盖 7 条 API 回归用例，任务创建、配置更新、状态检查、重启链路都具备基础自动化保护。
- 合并主分支后的第一批修改已经通过 `python -m pytest tests/api/test_task_routes.py` 验证，当前结果为 `7 passed`。
- 阶段 5 已从“建立 pytest 骨架”继续推进到“持续补关键接口回归”，可以继续往模板、重启配置和详情编辑链路扩展。
