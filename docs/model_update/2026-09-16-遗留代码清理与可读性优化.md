# 2026-09-16 遗留代码清理与后端可读性优化

本次为全仓一次性整理：文档整理 + 历史遗留代码删除 + 后端可读性优化，行为不变（不做架构重构、不改数据库、不改接口契约）。整体一个提交。

## 一、删除的历史遗留代码

### 仓库级遗留物
| 对象 | 说明 |
|---|---|
| `__pycache__/googlesheet.cpython-{313,39}.pyc`、`__pycache__/logger.cpython-{313,39}.pyc` | git 误跟踪的编译产物，对应早已删除的根级 googlesheet.py / logger.py；.gitignore 已覆盖该目录 |
| `frontend.zip`、`templates.zip`（根目录） | 前端重构前的历史快照；官方留档是 `docs/design/frontend-refactor/archive/templates-jinja-source.zip`（保留不动） |
| `PERFORMANCE_ANALYSIS.md`（根目录） | 自研表格版时代的性能分析，对应代码（weight_combination_v2.html、performance_analysis.py 路由）已删除，全仓 0 引用 |
| `docs/TABULATOR_INTEGRATION_COMPLETE.md` | Tabulator 集成已落地为事实（weight_combination.html 直接加载 tabulator），文档自带死链且描述的 v1/v2 双版本已不存在，全仓 0 引用 |
| `static/js/components/HighPerformanceTable.js` + `static/css/components/HighPerformanceTable.css` | 665 行虚拟滚动表格组件，已被 Tabulator 取代，全仓 0 引用 |

### 测试区遗留物
| 对象 | 说明 |
|---|---|
| `tests/ETF资产总数.py` | 通篇注释掉的 yfinance/akshare 探索草稿；功能已产品化为 `app/utils/etf_total_assets.py` + `tests/unit/test_etf_total_assets.py` |
| `tests/batch_register_models.py` | 向远程生产 API 批量注册导航的一次性脚本，**文件头部内嵌硬编码 JWT Token**（违反 AGENTS.md 凭据红线；Token 已过期但建议按泄露处理，轮换密钥） |
| `tests/test_sdk_client_bool_encoding.py` | import 已不存在的 `app.repositories.sdk_client.StockSdkAdapter`，无法运行 |
| `tests/scripts/benchmark_xpl_concurrency.py`、`benchmark_xpl_threads_from_d.py`、`debug_scaled_return_drawdown.py` | import 已改名的 `app.services.xpl_service`，按现名失效 |
| `tests/demos/flask_rbac_demo/instance/` | 残缺副本（完整版在 `tests/archive/demos/`），tests/demos/ 目录随之移除 |

### 归档（移动到 tests/archive/，删除功能但保留历史可查）
- `tests/多进程计算多产品最优比例.py` → `tests/archive/`（依赖未声明的 tqdm 的一次性权重搜索实验脚本）
- `tests/demos/{ce.py, excess_df.csv, excess_return.png}` → `tests/archive/demos/`（matplotlib 画图草稿及产物）

### 刻意保留（非遗留，有明确存在理由）
- `run_smoke.py`：ponytail P11 剩余子批（c5/c7 detail、c4、c3）仍将其列为冒烟方法，收尾后再删
- `docs/design/frontend-refactor/archive/templates-jinja-source.zip`：Jinja 版官方回滚兜底
- `tests/archive/`：制度化历史备份区（pytest norecursedirs 排除）
- `normalize_weighting_mode` 的 `mode`/`legacy` 形参：15 处调用方签名兼容，仅简化函数体与 docstring
- 启动期数据/路径兼容（startup.py 441/651/767）、`delete_legacy_performance_analysis_jobs` 遗留表清理、legacy_bp 旧书签路径（审计 D5 决议）、config_manager 旧 str(True/False/None) 数据兼容层

## 二、后端可读性优化（行为不变）

### 未使用 import 清理（pyflakes 全量核实）
- `app/services/google_sheet_tasks/c7.py`（11 个）、`c5.py`（10 个）、`c4.py`（4 个）：历史模板复制残留
- `app/services/task/runtime_view.py`（TaskLog/TaskResult/TaskResultReturn）、`app/services/strategy_backtest_report_service.py`（task_repository、TaskResult、TaskResultReturn）
- `app/services/kline_service.py`（timedelta、normalize_market_type、随死代码删除的 os）、`google_sheet_client.py`（traceback）、`config_manager.py`（Optional）、`backtest_multi_product_service.py`（get_backtest_model_version/get_price_type、两个 `_canonical_*` 死别名）
- `app/services/performance_analysis/`：metrics.py（Any/Dict/List）、portfolio_combiner.py（pandas、StrategyBacktestReportSchema）、result_mapper.py（json）、service.py（asyncio、calculate_v1_metrics）
- `app/utils/`：db_stock_api.py（time、Any）、ding_talk_notifier.py（Task、User）、qq_api.py（logging、Tuple）、value_parser.py（json）
- `app/routes/`：admin_api/task_api（jsonify）、auth_api/config_api/global_preview_api/navigation_api（request）、stock_api（ServiceError）、routes/__init__.py（Blueprint）
- `app/repositories/google_sheet_repository.py`、`google_sheet_token_repository.py`（NotFoundError）
- `app/schemas/backtest.py`（BaseModel，实际基类是 APIModel）
- 保留：`app/schemas/__init__.py` 的 noqa re-export、`backtest_multi_product_service.SUMMARY_ROW_DEFS`（被预览模块反向转引，已加 noqa 注释说明）

### 死代码 / 死赋值 / 恒等分支
- c3/c4/c5/c7 `check_result` 轮询后从未读取的 `_index_return_date/_start_return_date/_index_start_return_date/_kline_len/_end_data` 死赋值簇；c4 `n_plus_1` 分支恒真 `if i!=0` 与只被死赋值使用的 `_i` 级联清理
- `app/services/task/runtime.py`：`handle = self.submit_task_execution(...)` 返回值从未使用
- `app/services/google_sheet_client.py`：`last_exception` 死赋值（重试循环实际用 `from e` 链原始异常）
- `app/services/model_summary/extractor.py`：`normalized`、`index_profit_monthly_all`、`index_kama_all`、`index_sortino_all` 死赋值
- `app/services/backtest_report_query_service.py`：`start_kama_all`；`performance_analysis/service.py`：`google_sheet_url`；`text_analysis.py`：未使用的 `except as e`
- `app/services/kline_service.py:83`：STOCK_BASE_URL 环境变量回退恒无效（config.py 已在 init_app 镜像同一变量，唯一构造点在请求期），删除后与"单一来源：应用配置"的 docstring 自洽
- `app/services/google_sheet_tasks/c3.py`：`start_index = max(index_z, task.current_step - 1) if ...` 恒等于 `task.current_step`，化简；`get_bdl` 多余的第 5 形参 `index_z` 移除（唯一调用点传的就是 `task.current_step`，且与基类钩子签名对齐）

### 注释与命名
- `portfolio_combiner.py`：删除 8 行注释掉的旧双算法分支、3 处调试 CSV 导出注释、死常量 `LEGACY_WEIGHTING_MODE`；`normalize_weighting_mode` docstring 改为如实描述"恒返回默认算法、形参仅为兼容保留"
- `metrics.py`：中英双语自相矛盾注释（population vs 样本标准差）修正、空 `# TODO` 清理、注释掉的 `comparison_point` 旧写法删除
- `base.py`：`'year'/'Kline_key'` 同值落盘处补契约注释（'year' 被 C4 kline_range 消费，均为既有契约）
- `c3.py:494` 已废弃单股参数流说明注释、`c4.py:496` 历史行为注释保留
- `KLINE_DATA_MAP` → `kline_data_map`（base/c5/c7/backtest_training_service 共 40+ 处，形似模块常量实为局部传参）
- c3 `execute_task` 中注释掉的钉钉取消推送死代码删除
- 单字母变量改名：task_watchdog `t→thread`、export_workbook_service `r→result`/`f→value`
- 无占位符 f-string 清理（c3/c4/c5/c7/base/backtest_training_service/exporter）

### 去重
- `app/__init__.py` context processor 改调 `app/utils/auth.py::is_auth_enabled()`（消除与 auth.py 的逐字重复）

## 三、文档整理

| 文档 | 变更 |
|---|---|
| `docs/目录索引.md` | 补录 `指标计算与单品多品统一方案.md`、`design/vue-frontend-fix-2026-09/`；记录本次整理 |
| `README.md` | 后端目录树更新为现行结构（原树含已删除的 task_manager.py、api.py、google_sheet.py 等）；安装步骤的 `python run.py init-db / init-config` 修正为实际存在的 CLI：`flask --app run.py init-db / init-default-config / init-rbac`；前端架构补充 static/js 分层与 frontend/ Vue SPA |
| `docs/任务系统与调度.md` | `tests/legacy_services/` 过期引用改为 `tests/archive/legacy_services/` |
| `tests/archive/README.md` | 修正"tests/scripts 未入库"的过时说法（现均已入库）；登记本次移入的归档物与删除的三个失效脚本 |

## 四、已知问题登记（本次刻意不修，属行为变更）

1. **`app/routes/export_api.py:199`**：`getattr(g, "current_user", None)` 但本文件未 import `flask.g`，`GET /api/export/model-summary` 一调用即 NameError 500。修复只需补 import，但会改变线上行为（500→正常），建议单独提交。
2. **`ding_stream_service/task_commands.py:282`**：`list_tasks` 使用 `Task.query` 但未 import `Task`，钉钉 list 命令触发即 NameError。同上，单独修复。
3. **`tests/batch_register_models.py` 中的 Token**：已随文件删除出 HEAD，但仍存在于 git 历史；建议按凭据泄露处理并轮换。
4. `run.py` 监听 0.0.0.0（安全扫描提示）：既有部署行为，未改。
5. `app/services/task/runtime.py:735` finally 块内 return 的 SyntaxWarning：既有行为，未改。

## 五、验证

- `python -m compileall app/ ding_stream_service/ stock_sdk/ run.py` 通过
- pyflakes 对 `app/` 复扫：仅剩 3 个有意保留项（2 个 noqa re-export + export_api 的既有 `g` 缺失登记）
- 全量回归 `python -m pytest tests/unit tests/integration`：709 passed / 18 failed / 10 skipped；**18 个失败全部为存量失败**——已用 `git worktree` 在 pristine HEAD 基线复跑同一批用例（21 failed，含网络依赖用例的抖动），确认本次改动零新增失败
- 删除的测试文件均为失效/一次性脚本，正式测试集（unit/integration）不受影响
