# 全仓 ponytail 审计报告（2026-09-08）

> 定位：过度工程与复杂度审计（ponytail-audit），只列发现、不动代码。正确性/安全/性能不在范围内（范围外发现见 §F3）。
> 方法：4 路并行扫描（services / utils / routes+common JS / pages JS+外围），所有"死代码"结论均经全仓 grep 验证（app、tests、templates、static、frontend、scripts、run*.py）；对 3 项最激进结论做了人工复核（xpl_v2 无入口、database_api 零调用、export_service logger 缺失，均证实）。
> 说明：克隆率百分比为 diff 相同行数口径（含空行/括号行），实际逻辑重复率约低 10 个百分点。

## 总览

| 分区 | 规模 | 可削减（不含未决项） |
|---|---|---|
| app/（routes/services/repositories/schemas/utils） | ~35.9k 行 | ~2,300 行 |
| static/js/ | ~36.9k 行 | ~7,700 行（不含"随 Vue 迁移整体退役"口径） |
| frontend/ | ~14.3k 行 | 0～14,700 行（取决于 A1 双前端取舍） |
| stock_sdk/ | ~2.0k 行 | 0（已裁决：全保留） |
| 依赖 | — | -1（naive-ui，随 E10） |

**net: 约 -12,000～-14,000 行、-1 依赖可能**；其中无需产品决策、零行为变更的"闭眼删"约 **-2,900 行**。

---

## A. 重大决策项

### A1 [yagni/pick-one] 双前端并存
- `frontend/`（79 文件，14,315 行）完整复刻静态页全部路由与 API 封装（`frontend/src/router/index.js` 与 `static/js/pages/*` 路由一一对应；`frontend/src/api/*.js` 12 个文件重复 `static/js/common/api.js` 的端点表）。
- SPA 目前无产物服务线上：无 `dist/`；`Dockerfile:37-38` 只拷 `static` + `templates`；`nginx.conf:6` 指向占位路径（该文件本身即死样例）。
- 二选一：若 Vue 是终点（当前分支 dev_vue、AGENTS.md 称"主战场"），冻结静态 JS 新功能，本报告前端各条当迁移对照，不要先重构再退役；若静态页是产品，删 `frontend/` + `frontend.zip` + `nginx.conf`。
- 最多 -14,700 行；保底 -450 行（`frontend/src/views/Home.vue` 无人 import、`components/naive/*` 5 文件仅 1 个视图使用 + naive-ui 依赖、`nginx.conf`）。

### A2 [delete] xpl v1/v2 二选一 —— 已裁决：保留 v2，删 v1 链
- v2 JS 与 v1 有 1387/1457 行相同（95%）、HTML 942/968 行相同（96%）；导航无入口、唯一功能增量是 CSV 导出；v2 竟然请求 `/xpl/v1/analyze`（`static/js/pages/xpl_v2.js:611`）。
- **裁决（2026-09-08）**：保留 v2（含 CSV 导出），删除 v1 页面链。详见 02 决策文档 §1.2 与提示词 P03。

### A3 [yagni] yule 娱乐页无任何入口
- 路由存在（`app/routes/yule.py`）但 `app/navigation.py`、全部模板、SPA 均无链接，只能手敲 URL 到达。涉及 `templates/yule/` 2 文件 + `static/js/pages/yule_sjxz.js`（1,238 行）+ `yule_index.js`（1 行跳板）。
- 加导航或整体删除（约 -1,300 行）。未决。

### A4 stock_sdk 84% 未被 import —— 已裁决：全保留
- 全仓唯一消费点是 `app/services/kline_service.py:15`（只用 `stock_data` / `stock_data_us` 两个接口）；14 组 `param_*` API+模型、`sys_*` 全套（约 1,500 行）零引用，且 dataclass 与 `app/models.py` 重复声明同表。
- **裁决（2026-09-08）**：保留，不做任何删减。

### A5 [delete] 后端死接口群（前端零调用，grep 证实）
- `app/routes/database_api.py` 整文件（`/api/database/status`、`/api/database/suggestions`，24 行）：static/templates/frontend 零引用。删除后 `app/utils/db_monitor.py`（212 行）降级为仅测试使用（`tests/test_database_portability.py` 等），是否连带删除见提示词 P04 的两选项。
- `app/routes/admin_api.py:22-31` `/admin/api/scheduler/status`、`:89-99` `/admin/api/scheduler/cleanup`：唯一"调用方"是 `frontend/src/api/admin.js:5-6` 封装，无任何 Vue 视图 import。
- `app/routes/scheduler_api.py:88-92` `/api/admin/scheduler/tasks/<id>/status`：`admin_scheduler.js` 的 7 个调用均不含 `/status`；SPA 同样不用。
- `app/routes/task_api.py:220-236` `/api/tasks/<task_id>/system-logs` + `app/services/log_query_service.py:107-123` `query_task_system_logs`：唯一引用是 `frontend/src/api/task.js:17` 封装，无视图 import。
- `frontend/src/api/scheduler.js:11` `getSchedulerStatus` 指向已删除的 404 端点。
- 合计约 -300 行（含可选 db_monitor）。

---

## B. services（约 -1,400～-1,600 行）

已核查、**约定不动**：task/facade 拆分、BaseGoogleSheetService 钩子骨架、统一响应信封、repository 分层（AGENTS.md 明文）；`metrics.py`(1789 行) 公有方法均有内部调用；`backtest_excel_service`/`word_export_template`/`summary_contract`/`check_policy`/`kline_prep`/`historical_metrics`/`portfolio_combiner`/`backtest_multi_product_preview` 均为活代码。

### B1 [shrink] C5/C7 `_get_all_parameters` 同一 ~155 行函数抄两遍
- `c7.py:666-834` vs `c5.py:330-484`，骨架 ~90% 相同（限额计算、K线拉取、stock_name upsert、`require_kline_rows`、start_date 收口、full_years 边界、`project_and_validate_write_ready`、n_plus_1 分支、recent/full 循环、去重）；C7 增量仅约 10%：随机价格投影、半年跨度、`include_ohlc=True`、`_expand_random_price_groups`。
- `kline_prep.py` 只抽走了 15 行投影步，其余 ~140 行仍是复制品。
- **裁决（2026-09-08）**：合一为共享实现，且 C5 同步获得半年跨度 + 随机价格能力。见提示词 P05。

### B2 [shrink] 20 项指标 "year==all" 汇总投影手写 5 遍
- `summary_contract.py` 的 SUMMARY_ROW_CONTRACT 本就是为此而生，但 5 处各自重写：`backtest_multi_product_service.py:522-609` `_derive_metrics`（~88 行）、`backtest_report_query_service.py:866-940` `_build_fallback_rows`（~75 行）、`performance_analysis/exporter.py:48-261`（~210 行）、`performance_analysis/result_mapper.py:47-202`、`model_summary/extractor.py:~745-800`。
- 收敛为一个 `project_summary_metrics(metrics)`（放 summary_contract 或 historical_metrics），exporter 只留格式化。约 -250～-350 行。

### B3 [shrink] 轮询写回骨架 4 处复制
- `set_googl_val` + `_validate_check_values` + 60 次轮询循环（刷新时刻 `[5,15,25,35]` 四处一致）：`c5.py:85-280`、`c7.py:339-589`、`c4.py:325-448`、`backtest_training_service.py:558-743`（单 Sheet 变体）。`_validate_check_values` 语义相同（与 initial 快照比对，未变化即未就绪）。
- 上提 base 轮询钩子（AGENTS"去重/断点在基类"的最后一笔大账）。约 -270～-300 行。高风险区，需专项会话 + 集成测试兜底。

### B4 [shrink] C3/C4 内联 30 键 analyze 映射
- `c4.py:96-134`、`c3.py:424-463` 各自内联 `analyze_result.get(key, 0)` 30 键映射，键表与 `result_payload.ANALYZE_RESULT_KEYS` 逐字一致；`build_analyze_fields()` 已存在且 c5.py:80/c7.py:334 在用。result_payload 注释里"C3/C4 不强行并入"指的是 D 列指标格，不覆盖 analyze 尾巴。约 -80 行。

### B5 [shrink] `performance_analysis/sheet_reader.py` 三分支逐字相同
- `:123-201` C7/C5/C4 三个分支仅 if 条件不同（25 行列清单/日期解析/`_data` 提取 ×3）。合成 `elif any(t in title for t in ('C7','C5','C4'))`。约 -50 行。

### B6 [shrink] 指标辅助函数家族复制
- `_derive_year_max_excess_drawdown` ×2（`backtest_multi_product_service.py:334-380` vs `backtest_report_query_service.py:738-789`，~90% 同，仅符号约定差异）；"找 year=='all' 条目" ×5（facade.py:21、multi:242、report_query:323、extractor:219、result_mapper:84）；`_year_key` == `_metric_year_key`。
- 归入 `performance_analysis/historical_metrics.py`（两个消费方已 import 它）。约 -90 行。

### B7 [shrink] 百分比/数字容错解析 7 处变体
- `report_query.py:203-224`、multi:305-318、`export_file_service.py:757-779`、`result_payload.py:71-85`、`c3.py:176-190`、`c4.py:356-372`、`check_policy.normalize_check_values` —— 全是"去 %、去逗号、转 float"。扩展 `app/utils/value_parser.py`（既定 util）统一；check_policy 的 Sheet 报错语义保留。约 -60～-80 行。

### B8 [shrink] 杂项 helper 逐字复制
- `_max_yearly_repair_days` ×3（report_query:716、PA/exporter:37、extractor:727）；`_normalize_scientific_text` ×2（report_query:74 == extractor:186）；`_parse_json` ×2；`_strip_html_tags` ×2；Excel sheet 名清洗/唯一名循环 ×4。归 `app/utils/` 文本/数字模块。约 -60 行。

### B9 [shrink] 错误摘要块 4 处逐字复制
- "record_task_exception → format_task_error_message → fallback"：`base.py:686-696`、`backtest_training_service.py:234-244`、`backtest_multi_product_service.py:808-817`、`c3.py:567-577`。收进 `task/error_handling.py` 一个 helper。约 -40 行。

### B10 [delete] strategy 回测服务死包装
- `strategy_backtest_report_service.py:207-210` `_cumulative_to_daily`、`:212-215` `_daily_to_cumulative`、`:165-171` `_normalized_weights` 全仓零调用；`:23-28` 的 3 个 import 仅为它们存在。约 -20 行。

### B11 [delete] `performance_analysis/__init__.py` 无人使用的再导出
- `calculate_v1_metrics`/`combine_product_returns`/`normalize_weight` 再导出零消费（各消费方都直接 import 子模块）。缩到只剩 docstring。约 -8 行。

### B12 [shrink] response_dto 死分支与冗余 DTO
- `MetricsV1ResponseDTO`（response_dto.py:11-19）：1 产 1 消，只是丢 schema_version 的再包装，并入 `MetricsV1Result`；`to_json_dict(include_series=True)` 分支 + `frame_rows`（:46-63）4 个调用方全传 False。约 -35 行。

### B13 [delete→merge] xpl 双模块二合一
- `xpl_service.py`（5 行纯转发，26 个引用方）→ `performance_analysis_service.py`（真身 27 行，仅 2 个引用方 + 结构测试）。把 XPLAnalyzer 挪进 `xpl_service.py`，删真身文件，改 2 处 import + 结构测试。约 -27 行 + 消除"performance_analysis_service 里住着 XPL 分析器"的误导命名。

### B14 [shrink] model_summary 两文件装一个类
- `model_summary_service.py`（16 行 shim，5 个引用方）+ `model_summary/facade.py`（32 行，唯一引用方是 shim）。合一。约 -15 行。

### B15 [shrink] export_file_service 注册表
- Protocol + 4 类中 3 个 `build()` 逐字相同（:267-298、:917-932，仅 worksheet builder 不同）；`get_task_result_exporter` 仅 1 个调用方。换 `{task_type: worksheet_builder}` 字典 + 一个函数。约 -40 行。

### B16 [shrink] c5/c7 custom-kline 函数对重复
- `_get_custom_kline_data`（c5.py:282-310 vs c7.py:591-647，c7 为 OHLC 超集）、`_get_custom_parameters`（c5.py:312-328 == c7.py:649-664，仅 c5 的提前去重有行为差，c7 在 :833 补做了）。并入 base/共享模块，c5 传 `include_ohlc=False`。注意 AGENTS 约束：custom 分支不进 `_get_all_parameters`。约 -30 行。

### B17 [yagni] xpl_analysis_service 无用元组
- `_parse_runtime_params` 返回 `(dto, None)`，两个调用方（:52、:65）都只取第一个。直接返回 dto。约 -6 行。

### B18 [shrink] stock_meta 双写路径
- `stock_search_service.save_metadata`（:150-165）自建 dict + `bulk_upsert`，绕开唯一的 `stock_metadata_service.upsert_stock_metadata_in_session` 归一口。归一（涉及归一语义选择，列决策项）。

### B19 [shrink] model_summary/query 双重转换
- `query.py:191+198` 对同一行先全量 `_record_to_dict` 再对分页子集重转。复用 `records[start:start+per_page]`。（性能小修，顺手项。）

---

## C. utils（约 -450～-520 行）

已核查、**不动**：`kline_validation` 与 `kline_adjustment` 职责正交；`market.py`/`return_series.py`/`task_error_utils.py`/`c7_result_normalizer`/`backtest_report_metadata`/`result_validator`（主体）全部存活。

### C1 [shrink] 五个行情客户端复制 5 套子系统
- `qq_api.py`(411) / `dfcf_api.py`(360) / `akshare_api.py`(205) / `yf_api.py`(198) / `db_stock_api.py`(165)，合计 1,341 行：
  - 手写重试退避 ×3（qq_api._get:146-194、akshare_api._call_with_retry:45-58、db_retry._retry_operation:34-64）——tenacity 已在 requirements 且 dfcf 已在用（dfcf_api.__get:79-110 是范本）；`yf_api.py:9` import 了 tenacity 却从未调用。
  - Session+UA 构建 ×3（qq_api:29-35/89-106、dfcf_api:36-45/56-71、db_stock_api:30-36）。
  - 节流 ×2 逐字（qq_api._throttle:108-114 vs akshare_api._throttle:37-42）。
  - `ADJUST_TYPE_MAP` 逐字 ×2（qq_api:72-79 == akshare_api:25-32），且与 `kline_adjustment.py` 的别名表重复。
  - K线行构造+涨跌幅/振幅计算 ×4（qq_api._parse_kline_item:336-409、dfcf_api._parse_kline_data:177-209、yf_api 行循环:130-184、akshare ×2）。
  - `akshare_api._fetch_sina_daily`(128-147) 与 `_fetch_hk`(149-169) 近似相同。
- 抽 `build_stock_session()/request_with_retry()/build_kline_row()` 共享件 + `kline_adjustment.sina_adjust()`。约 -260～-330 行。
- 附注：proxy_manager 实际只被 dfcf 一家使用；`stockapi.stplan.cn` 存在 3 套 HTTP 栈（proxy_manager、db_stock_api、stock_sdk/http_client）。

### C2 [delete] qq_api 假代理桩
- `qq_api.py:20-26` 本地 no-op `configure_session_proxy/get_proxy_for_request` + :95/:153/:157 假管道，全仓零外部调用。约 -9 行。

### C3 [delete] db_stock_api 死方法
- `insert_stock_template_param`（:105-125）生产零调用（仅 tests/test/ 旧脚本）；`get_single_stock_template_param`（:78-103）+ `base.py:884-890` 包装——c3.py:528 注释已宣告该流废弃（stock_param 恒 None）；`close()`（:161-164）零调用；`_make_request` PUT/DELETE 分支无调用方。约 -60 行。

### C4 [shrink] alert_decorator 与 ding_talk_notifier 重复
- 任务 URL 拼接 ×3（alert_decorator._resolve_task_url:41-51、ding_talk_notifier._task_detail_url:54-62、base.py:190）；errcode 处理 ×2（alert_decorator:84 vs notifier:221）；`_default_message_builder`（:54-63）零使用（5 个调用方全传 `build_execute_task_alert`）。
- `send_failure_alert` 直接委托 `notifier.send_task_notification(task_id, notify_type="error", ...)`。141 行 → ~90 行。tests/test/ 下引用已不存在模板的死测试一并清理。

### C5 [delete] yf_api 死重量
- `:3,4,8,9` 未使用 import（hashlib/time/requests/tenacity）；多 ticker 分支永不触发（`get_kline_data` 恒单标的；`_normalize_ticker_hint` 的 list/tuple/set 分支死）；`proxy=None` 参数从未被读。保留 MultiIndex level 提取。约 -30 行。

### C6 [shrink] db_retry / database 收缩
- `database.py transaction_required`（:26-44）三个 except 分支做同一件事，合一；`DatabaseRetryManager` 类壳（db_retry:101-125+157）仅服务一个模块全局且只是转发 `safe_db_operation`，降为函数（调用点 `repositories/base.py:68` + 测试）。约 -23 行。`db_monitor.py` 存活（见 A5 连带说明）。

### C7 [delete] 杂项死代码
- `task_types.KNOWN_TASK_TYPES`（:7-15）无生产引用且 `normalize_task_type` 不查它（仅 tests/unit/test_value_parser_and_task_types.py:8 引用，测试同步改）；`value_parser.parse_ratio`（:48-50）纯别名仅 1 个调用方（strategy_backtest_report_service.py:22）；`market.market_type_from_eastmoney`（:125-127）兼容别名 4 个调用点改 `resolve_market_type`；`result_validator.py:52-53、102-106` 注释代码。约 -19 行。

### C8 [shrink] 布尔解析 4 套
- `config_manager.coerce_bool`（:99-119，主）、`dfcf_api._should_use_proxy_for_kline`（:73-77）、`app/config.py:6 _get_bool`、`portfolio_combiner.py:38 _coerce_bool`。除主入口外统一走 `coerce_bool`。约 -12 行。另 `value_parser._convert_pandas_to_native`（:70-89）可缩为 `json.loads(df.to_json(orient="records"))`（约 -15 行，4 个调用方在 performance_analysis/）。

---

## D. routes（约 -450 行 + 决策项）

`app/routes/__init__.py` 31 个蓝图注册均指向活路由，无死注册。

### D1 [shrink] 9 个页面路由文件 = 同一 4 行函数复制 34 遍
- `admin.py`(85)、`backtest_training.py`(51)、`backtest_multi_product.py`(46)、`google_sheet.py`(56)、`xpl.py`(99 行中 39 行为页面路由)、`yule.py`(23)、`global_preview.py`(22)、`eastmoney_kline.py`(13)、`auth_pages.py`(12)、helper `page_files.py`(15)。`grep -c "return send_page"` → 34 个单行函数体。
- 换 PAGE_TABLE + `add_url_rule` 循环（347 行 → ~75 行）；仅保留 google_sheet 两个版本 dispatcher 手写与 xpl 的 2 个 analyze API。约 **-270 行**。

### D2 [shrink] backtest_api.py 双蓝图端点成对重复
- `import_excel`(73-85) == `bmp_import_excel`(282-293)；`get_task_results_by_task_id`(88-115) vs (296-315) 仅差任务守卫与 `_parse_json`；`get_global_preview`(207-216) vs (375-382) 仅差 loader。一函数注册两蓝图 + 按 task_type 分发。约 -45 行。

### D3 [shrink] result_api 一个 URL 两种响应形状
- `result_api.py:20-42`：带 `page&per_page` → 分页载荷；不带 → 裸 `{"items":[...]}`。两侧均有活调用方（`task-polling.js:90` 分页；`google_sheet_c4_detail.js:669` 不分页）。定一种形状需迁移一端客户端。列决策项。

### D4 [yagni] `/api/google-sheet-tokens/reconcile` 无 UI 调用方
- `google_sheet_api.py:111-117`，注释自称"显式维护路径"。若运维确在 curl 则保留并写入运维文档；否则删（service 层 `reconcile_in_use_counts` 保留，startup 在用）。列决策项。

### D5 [delete-or-keep] 旧书签兼容蓝图
- `backtest_training.py:46-51`（`/backtest/*`）、`backtest_multi_product.py:42-46`（`/backtest-multi/*`）仓内零引用，且 `startup.py:616-629` 已在 DB 层归一旧导航路径。删除会使外部浏览器收藏夹 404。列决策项（倾向保留，仅 12 行）。

### D6 [shrink] xpl.py analyze 对同体
- `analyze_data` 与 `analyze_data_v1`（:41-99）同一 8 行"parse → service → ok?success:error"体。抽 `_run_analyze(service_fn)`，URL 全保留（wire 兼容）。约 -15 行。
- 注意：A2 删 v1 页面后 `/xpl/v1/analyze` API 必须保留（v2 页在用）。

### D7 死接口群 = A5（P04 执行）。

### D8 [yagni] `/api/meta/versions` 硬编码清单
- `meta_api.py:17-29`，唯一调用方 `frontend/src/views/Home.vue:31`（SPA 未构建上线）；`create_url` 表与 `app/navigation.py` 导航数据重复维护会漂移。并入 `/meta/enums` 或由导航表派生。约 -13 行。

### D9 legacy 注册（见 D5）。

---

## E. 静态 JS（约 -7,700 行，不含"随 Vue 迁移整体退役"口径）

每个 `static/js/pages/*.js` 都被恰好一个模板引用（已验证），无孤儿页面文件——钱全在复制粘贴里。

### E1 [shrink] c4/c5/c7 create+detail 克隆家族（F3 共享化半途而废）
- c5↔c7 create：**1501/1542 行相同（97%）**（差异仅 C7 模型版本块与 `c5`→`c7` 串改名）；c5↔c7 detail：**1365/1415（96%）**。孪生函数约 20 个仍逐字留在页面：`loadWorksheetsForItem`、`loadGoogleSheetsForItem`、`applyConfigToForm`、`bindEvents`、`addVisualSheetConfigItem`/`removeLastVisualSheetConfigItem`、`syncProductCodesToParam1`、`addProductCodesFromInput`、`removeProductCode`、`calculateCombinations`、`showCombinationPreview`、`submitTask`、`getCurrentConfig`、`normalizeC4Config`、`loadRestartConfig`、`loadTemplates`、`fillFormWithTemplate`、各页私有第三份 `debounce` 等。
- c4 与 c5/c7 create 相同 57%（833/1452）；detail 四文件（含 c3 的 `google_sheet_detail.js` 1488 行）共享 ~30 个同名函数（`cancelTask`/`startAutoRefresh`/`checkTaskStatus`/`restartTask`/`groupResults`/`renderResults`/`showResultDetail`/`loadTaskDetail`/导出四件套 ×3 份…）。
- 按既有 `Biz.*` 模式收尾（`common/business/`，IIFE 挂 window.Biz，无构建步骤），每页只留版本差异。家族合计 6,921 行 → 约 2,400 行版本专属 + 共享件，**约 -4,500～-5,000 行**。

### E2 [shrink] backtest_training ↔ backtest_multi_product 三对克隆
- detail：**1133/1239 相同（91%）**（40+ 同名函数：`groupSummaryRows`、`buildPaginationPages`、`applyC3MetricSelection`、`formatC3MetricValue`、`inferModelVersion`、`handleRestartFromScratch/ResumeRestartTask/StopTask`…）；result：**847/897（94%）**（`renderCharts`/`renderSharpeCompare`/`renderProfitMonthly`/`exportV1Details`/`exportWordReport`/`buildBarChart`…）；list：**427/467（91%）**。create 仅 21% 相同（不动）。
- 按 single/multi 模式旗标参数化（multi 增量：`inferProductModelVersion`、`copyAllTaskParameters`、`renderTabDailyReturns`）。约 -1,900～-2,100 行。

### E3 [shrink] 公共工具各页私设
- `escapeHtml` **24 份定义**（template-auth.js、business/task-list.js:40、result-table.js + pages/ 21 文件，各 ~7 行）；`formatTime` ×4；`debounce` ×4（template-auth.js:240 + c4/c5/c7 create 各一）；`showAlert` ×4；`getStatusText` ×2 且文案已分叉（template-auth.js:183 `running:"执行中"` vs task-list.js:68 `running:"运行中"`——合并需先定文案，行为决策）。
- 收敛进每页已加载的 `common/utils.js`（现仅 32 行）。约 -200 行。

### E4 [shrink] 双 fetch 栈 + 54 处手写 URL
- `template-auth.js:322-390`（authFetch/requestJson/ajaxRequest）与 `api.js:15-47`（Api.request 信封）并存；13 个页面文件绕过 api.js 手写 54 处 `/api|/admin/api` URL（AGENTS"接口唯一出口"合规率约 60%）。大户：`admin_tasks.js`(13)、`admin_users.js`(7)、`admin_scheduler.js`(7)、`admin_roles.js`(5)、`admin_logs.js`(5)。
- api.js 增加 admin 域端点表 → 迁移 13 页 → 删 template-auth 的 requestJson/ajaxRequest（~35 行）。约 -60 行 + 单一链路。

### E5 [delete] Biz 共享模块死函数
- `task-submit.js:18-29 clearAllParameters`、`:87-96 handleTaskEvent`（`saveAsTemplate`/`submitTemplate`/`generateCombinations` 存活，勿动）；`task-polling.js:24-42 manualRefresh`（页面自绑监听 + google_sheet_detail.js:74 私有副本，Biz 版零调用）；`utils.js:6-12 sanitizeJSONString`。约 -40 行。

### E6 [shrink] template-auth.js 自我死重
- `isAuthEnabled()` 硬编码 true（:94-98）却留 3 个死分支（:702-705、:755-760、:216-218 mock 支路）；`templateUnsupportedPaths` 空 Set（:27）；`legacyPathMap`（:16-25）与 `startup.py:616-629` 服务端归一表重复维护。约 -30 行。
- 已核查非重复：template-auth 与 admin-shell.js 职责正交；JWT fetch 拦截不进 api.js 是文档化决定（api.js:3-4）。

### E7 [shrink] c31 ↔ c3 create 共享核心
- `google_sheet_c31_create.js`(1897) 与 `google_sheet_create.js`(1297) 共享 659 行；token 选择器块 ~200 行逐字可搬（`syncSelectedTokenMeta`/`applyPendingTokenSelection`/`renderGoogleSheetTokenOptions`/`loadGoogleSheetTokens`/`importGoogleSheetToken` 等 16 个函数）。c31 真正独有的是多 Sheet 批量 UI + 年份区间模式（~40%）。约 -400～-500 行。

### E8 [shrink] 详情页轮询/格式化样板
- `startAutoRefresh` ×6、`formatDuration` ×4、`getProgressBarClass` ×4、`checkTaskStatus` ×4、`downloadBlob` ×3、`formatNumber/formatPercent` 手写 ×3（eastmoney_kline_index.js:31-40、xpl_index.js:31-40 等）。`common/business/task-polling.js`(245) 已覆盖但仅 c4/c5/c7 detail 模板加载；c3 与 backtest 详情页各玩各的。约 -700～-900 行（与 E1/E2 部分重叠，独立价值在 c3 + backtest 页与 24 份 escapeHtml）。

### E9 [shrink] yule_sjxz 五个动画引擎同一骨架
- `startCardsAnimation`(383)/`startBouncing`(469)/`startWaterfall`(559)/`startFlip3D`(634)/`startStars`(725) 共享 pick/iterate/DOM 骨架与 `completeAnimation/showResult/saveToHistory` 尾巴；另私设 `showNotification`(1210) 与 template-auth.js:138 重复。随 A3 决策联动；不删则合一引擎。约 -250～-300 行。

### E10 [yagni] frontend/ 内部
- `views/Home.vue`(38) 无人 import；`main.js` 同时注册 element-plus 与 naive-ui，`components/naive/*` 5 文件仅 `TaskListInner.vue` 一个视图消费——移植该视图后可整体弃用 naive-ui（约 -400 行 + 1 依赖）。随 A1 决策联动。

---

## F. 外围与卫生

### F1 [delete/archive] tests 根目录杂物 + 一次性脚本
- `tests/T_word.py`（import 不存在的 `d` 模块）、`tests/计算指数.py`、`tests/test/` 抽屉（`xpl_service copy.py`、`te.py`、`A.py`、`xpl.html`、`.cache.txt`）。pytest 仅收集 `test_*.py`，但 `tests/test/test_value_parser.py` 正被真实收集且住在杂物堆里——迁 `tests/unit/`，其余迁 `tests/archive/`（既有约定）。
- `scripts/backfill_metrics_payload.py`（243 行）为 2026-08 指标载荷修复的一次性脚本，生产回放完成后归档。
- ding_stream_service 无死代码；但 `task_commands.py:10-12` 直接 import 单体 `create_app/task_repository/task_manager`——是第二入口而非边界，README 表述略过头（不动代码）。

### F2 [doc] AGENTS.md 过时表述
- "run.py/startup.py 里还有运行时 ALTER TABLE 补列"：实际 `run.py` 仅 27 行干净入口，schema 修补只在 `app/startup.py:43-360`（~320 行）一处 + 一个迁移 + tests/scripts 修复工具。无重复代码，仅文档误导。

### F3 [范围外 bug，走正常修复] export_service logger 缺失
- `app/services/export_service.py:359` 调用 `logger.exception(...)` 但模块从未定义/导入 `logger`——`_stream_stock_zip` 异常路径将抛 `NameError` 掩盖原始导出错误。补 `logger = get_logger(__name__)`。

---

## 净值

- **无需决策、零行为变更（闭眼删）**：services ~1,400 + utils ~450 + routes ~450（含 A5 死接口）+ JS 公共层 ~330 + tests 杂物 ~250 ≈ **-2,900 行**。
- **三项裁决内**：B1（C5/C7 合一 + C5 同步）已裁决执行；A2（xpl 保留 v2 删 v1）约 -2,400～-2,900 行；A4（stock_sdk）保留不动。
- **未决**：A1 双前端（0～-14,700）、A3 yule（~-1,300）、E1/E2 克隆家族（~-6,400～-7,100，若 Vue 定为终点则转为"迁移对照、整体退役"，勿先重构再退役）、E10 naive-ui（-400 行 -1 依赖）、D3/D4/D5/D8/B18 小决策项。
