# 执行提示词集（2026-09-08）

> 用法：每条提示词自包含，可整段粘贴到新会话执行。条目编号对应 [01-审计报告.md](01-审计报告.md)，批次依赖与推荐顺序见 [02-决策与执行计划.md](02-决策与执行计划.md) §2。
> 通用后缀（每条提示词已内置，无需重复输入）：完成后运行 `python -m pytest tests/unit tests/integration` 全绿；不要主动 commit；发现与描述不符时先报告再动手。

---

## P01 utils 死代码快删（约 -130 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中执行一批已 grep 证实零引用的死代码删除（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §C2/C3/C5/C7）。逐项执行，每项删前先自行 grep 复核引用确为零：

1. app/utils/qq_api.py:20-26 删除本地 no-op 代理桩 configure_session_proxy/get_proxy_for_request，并清掉 :95/:153/:157 处对它们的假管道调用（这些函数全仓仅在 qq_api 内部出现）。
2. app/utils/db_stock_api.py：删除 insert_stock_template_param(:105-125)、get_single_stock_template_param(:78-103)、close()(:161-164)，_make_request 只保留 POST 分支；同时删除 app/services/google_sheet_tasks/base.py:884-890 对 get_single_stock_template_param 的包装函数及其引用（c3.py:528 注释已宣告该流废弃，stock_param 恒为 None）。注意 base.py 其余对 db_stock_api 的使用（如 :800 的 POST）必须保留。
3. app/utils/yf_api.py：删除未使用 import（hashlib、time、requests、tenacity 整行，:3,4,8,9）；删除永不触发的多 ticker 分支与 _normalize_ticker_hint 的 list/tuple/set 死支路（get_kline_data 恒传单标的；保留 MultiIndex level 提取逻辑）；删除从未被读取的 proxy=None 参数（调用方在 app/services/kline_service.py，同步去掉实参）。
4. app/utils/task_types.py：删除 KNOWN_TASK_TYPES 常量（normalize_task_type 不查它）；同步修改 tests/unit/test_value_parser_and_task_types.py 中对它的引用。
5. app/utils/value_parser.py：删除 parse_ratio 纯别名（:48-50），唯一调用方 app/services/strategy_backtest_report_service.py:22 改用 parse_percent_like。
6. app/utils/market.py：删除 market_type_from_eastmoney 兼容别名（:125-127），4 个调用点改用 resolve_market_type。
7. app/utils/result_validator.py：删除注释代码块（:52-53、:102-106）。

完成后运行 python -m pytest tests/unit tests/integration 确认全绿，汇报每个文件的删减行数。不要 commit。
```

## P02 services 死代码快删（约 -105 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中执行一批 services 层死代码删除与模块合一（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §B10-B14）。每项删前先 grep 复核：

1. app/services/strategy_backtest_report_service.py：删除零调用包装 _cumulative_to_daily(:207-210)、_daily_to_cumulative(:212-215)、_normalized_weights(:165-171) 及 :23-28 仅为它们存在的 import（cumulative_to_daily、daily_to_cumulative、normalize_weight）。注意 _combine_product_returns 直接使用的 combine_product_returns 是活代码，勿动。
2. app/services/performance_analysis/__init__.py：删除无人使用的再导出（calculate_v1_metrics、combine_product_returns、normalize_weight——各消费方均直接 import 子模块），只留 docstring。
3. app/services/performance_analysis/response_dto.py：MetricsV1ResponseDTO(:11-19) 并入 MetricsV1Result（它只丢 schema_version；生产方 result_mapper.py:239、消费方 strategy_backtest_report_service.py:40 两处同步）；删除 to_json_dict 的 include_series 分支与 frame_rows(:46-63)（全部 4 个调用方均传 False，删参数后调用方同步去掉 False 实参）。
4. xpl 模块二合一：app/services/performance_analysis_service.py（27 行，XPLAnalyzer 真身）整体并入 app/services/xpl_service.py（5 行转发 shim，26 个引用方——留这个人人都在 import 的名字），删除 performance_analysis_service.py，改 2 处 import（app/services/strategy_backtest_report_service.py:16 与 tests/unit/test_performance_analysis_structure.py:1；若结构测试断言模块路径，按新布局更新断言）。
5. model_summary 合一：app/services/model_summary/facade.py（32 行）内容并入 app/services/model_summary_service.py（16 行 shim，5 个引用方：app/routes/admin_api.py:9、app/services/export_service.py:36、app/services/google_sheet_tasks/base.py:783、2 个测试），删除 facade.py。

完成后运行 python -m pytest tests/unit tests/integration 确认全绿。不要 commit。
```

## P03 xpl 保留 v2、删除 v1 页面链（约 -2,400～-2,900 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中执行已裁决的 xpl 版本收敛（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §A2 与 02-决策与执行计划.md §1.2）：保留 v2 页面（含其 CSV 导出），删除 v1 页面链。

删除：
1. app/routes/xpl.py 中 /xpl/v1 页面路由（约 :30-36 区域，返回 send_page('xpl/v1.html') 的那个函数；保留 /xpl、/xpl/v2 页面路由和全部 analyze API）。
2. templates/xpl/v1.html、static/js/pages/xpl_v1.js、static/css/pages/xpl_v1.css（若存在）、对应 v1 页面的集成测试（grep tests/ 下引用 xpl/v1 或 xpl_v1 的测试文件）。
3. 保留 /xpl/v1/analyze 与 /xpl/v2/analyze API——v2 页面（static/js/pages/xpl_v2.js:611）调用的是 /xpl/v1/analyze，纯历史命名，严禁删除。

同步修改：
4. app/navigation.py：/xpl/v1 导航项改指 /xpl/v2，标签去掉"V1"字样（如"V1 回测数据分析"→"回测数据分析"）。
5. 全仓 grep '/xpl/v1'、'xpl_v1'、'xpl/v1.html'，清理 templates/、static/ 中指向 v1 页面的入口链接（xpl_index 相关页面改指 v2）；static/js/common/api.js 中仅是注释提及的可顺手更新注释。
6. frontend/src（SPA）内的 xpl/v1 Vue 视图不在本次范围，勿动。

完成后运行 python -m pytest tests/unit tests/integration 确认全绿；列出所有被删/被改文件。不要 commit。
```

## P04 routes 死接口群删除（约 -100～-300 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中删除一批前端零调用的死接口（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §A5；每项删前先 grep 复核 static/、templates/、frontend/src/ 确无调用）：

1. 整文件删除 app/routes/database_api.py（/api/database/status、/api/database/suggestions），并移除 app/routes/__init__.py 中对应注册。db_monitor（app/utils/db_monitor.py）暂保留（tests/test_database_portability.py 仍引用），在汇报中注明它已无生产调用方。
2. app/routes/admin_api.py：删除 /admin/api/scheduler/status(:22-31) 与 /admin/api/scheduler/cleanup(:89-99) 两个端点；同步删除 frontend/src/api/admin.js 中无人调用的封装 getAsyncTaskStatus、cleanupAsyncTasks。
3. app/routes/scheduler_api.py：删除 /api/admin/scheduler/tasks/<id>/status(:88-92)；同步删除 frontend/src/api/scheduler.js:11 指向已删端点的 getSchedulerStatus。
4. app/routes/task_api.py：删除 /api/tasks/<task_id>/system-logs(:220-236)；若 grep 证实 query_task_system_logs 仅被该路由使用，则连删 app/services/log_query_service.py:107-123 该方法；同步删除 frontend/src/api/task.js:17 的 getTaskSystemLogs。

注意不要误删 scheduler_api.py 其余端点、admin_api 其余端点（均存活）。完成后运行 python -m pytest tests/unit tests/integration 确认全绿。不要 commit。
```

## P05 C5/C7 `_get_all_parameters` 合一 + C5 同步半年跨度与随机价格（约 -150 行，高风险）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中合并 C5/C7 的参数展开主函数，并让 C5 获得 C7 的增量能力（用户裁决，背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §B1、02 文档 §1.1）。

现状：app/services/google_sheet_tasks/c7.py:666-834 与 c5.py:330-484 是同一约 155 行函数的两份拷贝（~90% 相同）：限额计算 (( _end_year-_start+1)*300)、K线拉取、stock_name upsert、require_kline_rows、start_date 收口、full_years 边界检查、project_and_validate_write_ready 调用、n_plus_1 分支、'recent' 循环、'full' 循环（parameters[1]×parameters[2] 叉乘）、去重。C7 增量仅约 10%：随机价格投影、半年跨度、include_ohlc=True、_expand_random_price_groups。

任务：
1. 把共享骨架抽为一份实现（放 google_sheet_tasks/base.py 或独立模块如 parameter_expansion.py，遵守 AGENTS.md"去重/断点在基类、子类经钩子接入、禁止复制骨架"；不要塞进 _get_all_parameters 的自动行情链路以外的位置）。用选项参数表达 C7 增量：支持半年跨度切分、随机价格组展开（_expand_random_price_groups）、include_ohlc。
2. C7 改为调用共享实现，行为必须与现状逐字段一致。
3. C5 接入同一实现并同步获得半年跨度与随机价格能力：C5 的配置中相应键缺省时走原 C5 路径（行为不变）；配置提供时按 C7 语义执行。先读 c7.py 的 _expand_random_price_groups 与半年跨度实现，确认 C5 接入所需配置键名与读取方式，并在汇报中列出 C5 新支持的配置键。
4. 附带项（同文件顺手做）：c5._get_custom_kline_data(:282-310)/c7._get_custom_kline_data(:591-647) 与 _get_custom_parameters 两对函数合一（c7 版是 OHLC 超集；c5 传 include_ohlc=False；c5 现有的提前去重与 c7 :833 的后续去重语义对齐后保留一份）。严格遵守 AGENTS.md：custom K线只在 get_bdl() 预计算阶段读 Sheet 输入列，不进 _get_all_parameters 自动行情链路。

验证：先 grep tests/ 下 c5/c7 参数展开相关测试并跑通；改动后全量 python -m pytest tests/unit tests/integration 全绿。若某项测试覆盖不足，为共享实现补一个参数化单元测试（覆盖 full/recent/n_plus_1/半年跨度/随机价格五条路径）。完成后汇报：共享函数签名、C5 新增配置键、两文件行数变化。不要 commit。
```

## P06 services 小合并合集（约 -190 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中执行六项低风险 services 收敛（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §B4/B5/B9/B15/B17/B19）：

1. B4：app/services/google_sheet_tasks/c4.py:96-134 与 c3.py:424-463 各自内联的 30 键 analyze 映射，替换为调用 app/services/google_sheet_tasks/result_payload.py 已有的 build_analyze_fields(analyze_result)（c5.py:80/c7.py:334 已在用；键表与 ANALYZE_RESULT_KEYS 逐字一致；result_payload 注释中"C3/C4 不强行并入"指 D 列指标格，不涵盖 analyze 尾巴）。
2. B5：app/services/performance_analysis/sheet_reader.py:123-201 的 C7/C5/C4 三个逐字相同分支合为一个 elif any(t in title for t in ('C7','C5','C4')) 分支。
3. B9：在 app/services/task/error_handling.py 新增 helper（如 summarize_task_exception），替换 4 处逐字相同的"record_task_exception → format_task_error_message → fallback"错误摘要块：google_sheet_tasks/base.py:686-696、backtest_training_service.py:234-244、backtest_multi_product_service.py:808-817、google_sheet_tasks/c3.py:567-577。注意保持 error_message 写入行为（含 [NETWORK_RETRYABLE] 前缀路径）完全不变——看门狗依赖该前缀。
4. B15：app/services/export_file_service.py 的 Protocol+4 类注册表中，C7/C5/C3 三个 build() 逐字相同（:267-298、:917-932），改为 {task_type: worksheet_builder} 字典 + 单一构建函数；get_task_result_exporter 唯一调用方在 build_task_export(:346)。
5. B17：app/services/xpl_analysis_service.py 的 _parse_runtime_params 返回 (dto, None) 改为直接返回 dto，两个调用方（:52、:65）同步。
6. B19：app/services/model_summary/query.py:191+198 对同一行做两次 _record_to_dict，分页路径复用 records[start:start+per_page]。

完成后运行 python -m pytest tests/unit tests/integration 确认全绿。不要 commit。
```

## P07 轮询写回骨架上提 base（约 -280 行，高风险）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中把四处复制的"写回+轮询"骨架上提为基类钩子（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §B3；这是 AGENTS.md"去重/断点在基类"欠的最后一笔大账）。前置：确认 P05 已完成（避免同区域冲突）。

四处同构代码：app/services/google_sheet_tasks/c5.py:85-280、c7.py:339-589、c4.py:325-448、app/services/backtest_training_service.py:558-743（单 Sheet 变体）。共同结构：set_googl_val 闭包（K线变化检查 → clear_range → 写日期/值单元格 → 20s 刷新参数）、check_result = normalize_check_values、_validate_check_values（与 initial_results 快照比对，未变化即未就绪）、60 次轮询循环（attempt % 10 == 0 or attempt in [5,15,25,35] 时刷新）、逐 sheet get_ranges([range1, check_positions])、merged_return_range 按 output_column_j/l 切分、_return_data 构建、get_return_analysis_v1 调用。

任务：
1. 在 app/services/google_sheet_tasks/base.py 增加轮询写回钩子（布局 dict、写回函数、校验函数、刷新时刻表作为参数/钩子注入），四处改为调用钩子；各处仅保留真差异（C7 的布局 dict、c7_0_3 指数收益、OHLC 写回；c4 的按市场取价；training 的单 Sheet 变体）。
2. 行为保持逐字段一致：刷新时刻表、快照校验语义、超时与失败路径、日志内容都不能变（任务系统核心链路，看门狗与断点恢复依赖现有行为）。
3. 改前先记录四个函数现有测试覆盖（grep tests/ 中 c3/c4/c5/c7/backtest_training 执行链路测试）；若覆盖不足，先补最小集成测试（mock google_sheet_client）再动骨架。

完成后运行 python -m pytest tests/unit tests/integration 确认全绿，汇报四处各自的行数变化与钩子签名。不要 commit。
```

## P08 指标投影与 helper 家族归并（约 -400～-500 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中归并五处复制的指标汇总投影与散装 helper（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §B2/B6/B7/B8）：

1. B2 汇总投影 ×5：新建 project_summary_metrics(metrics)（放 app/services/summary_contract.py 或 performance_analysis/historical_metrics.py，由 SUMMARY_ROW_CONTRACT 驱动），替换五处手写"year=='all' 投影 + excess_returns['all'] + index_sharpe_ratios['all'] + 月度超额均值"逻辑：backtest_multi_product_service.py:522-609 _derive_metrics、backtest_report_query_service.py:866-940 _build_fallback_rows、performance_analysis/exporter.py:48-261、performance_analysis/result_mapper.py:47-202、model_summary/extractor.py:~745-800。各处只保留自己的格式化差异（标签、百分号、精度）。
2. B6 指标 helper 归入 historical_metrics.py：_derive_year_max_excess_drawdown ×2（backtest_multi_product_service.py:334-380 与 backtest_report_query_service.py:738-789，~90% 同，差异仅在 _safe_number vs _parse_percent_like_value，参数化解析器后合一）；"找 year=='all' 条目" ×5（performance_analysis/facade.py:21 _all_entry、multi:242、report_query:323 _safe_all_entry、extractor:219、result_mapper:84 pick_all）合一为 all_entry()；_year_key(multi:321) == _metric_year_key(report_query:727)。
3. B7 容错解析：扩展 app/utils/value_parser.py 统一 7 处"去 %、去逗号、转 float"变体：report_query.py:203-224、multi:305-318、export_file_service.py:757-779、result_payload.py:71-85 to_decimal_ratio、c3.py:176-190、c4.py:356-372、check_policy.normalize_check_values（check_policy 的 Sheet 报错语义保留在原处）。注意保留 report_query._parse_percent_like_value 的符号循环语义。
4. B8 杂项：_max_yearly_repair_days ×3（report_query:716、performance_analysis/exporter.py:37、extractor:727）、_normalize_scientific_text ×2（report_query:74 == extractor:186）、_parse_json ×2（multi:233 == extractor:168）、_strip_html_tags ×2（report_query:137 == stock_search_service.py:177）、Excel sheet 名清洗/唯一名 ×4（report_query.py:1214、1327、export_file_service.py:810、model_summary/exporter.py:127）归入一个 app/utils 文本/数字模块。

验证：这些函数直接决定导出与报告数值。先跑现有相关测试（grep tests/ 中 multi_product/report_query/extractor/exporter 关键词）；为 project_summary_metrics 与统一的数值解析补参数化单测（含 %、千分位、负号、科学计数、空值样例）。全量 python -m pytest tests/unit tests/integration 全绿。不要 commit。
```

## P09 utils 行情客户端共享件 + 通知/重试收缩（约 -350～-420 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中收敛五个行情客户端的复制子系统并收缩通知/重试层（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §C1/C4/C6/C8；建议 P01 已完成）：

1. C1 五客户端共享件（qq_api.py/dfcf_api.py/akshare_api.py/yf_api.py/db_stock_api.py，合计 1341 行）：新建共享层（app/utils/ 下或 stock 客户端基模块）提供三件套——build_stock_session(referer=...)（Session+Chrome UA+no-cache，替换 qq_api:29-35/89-106、dfcf_api:36-45/56-71、db_stock_api:30-36 三份）、request_with_retry(...)（基于 tenacity，dfcf_api.__get:79-110 为范本，替换 qq_api._get:146-194 与 akshare_api._call_with_retry:45-58 两份手写退避）、build_kline_row(...)（标准 K线行 dict + pct_change/amplitude/change 计算，替换 qq_api._parse_kline_item:336-409、dfcf_api._parse_kline_data:177-209、yf_api:130-184 行循环、akshare 两处）。附带：节流合一（qq_api._throttle:108-114 == akshare_api._throttle:37-42）；ADJUST_TYPE_MAP 逐字两份（qq_api:72-79 == akshare_api:25-32）并入 app/utils/kline_adjustment.py 新增 sina_adjust()；akshare_api._fetch_sina_daily(128-147) 与 _fetch_hk(149-169) 合并为一个带 fetcher 参数的 helper。各客户端对外函数签名与返回结构严禁变化（kline_service.py 的 _fetch_* 直接依赖）。
2. C4 通知收缩：app/utils/alert_decorator.py 的 send_failure_alert 改为委托 app/utils/ding_talk_notifier.py 的 send_task_notification(task_id, notify_type="error", summary=message)，删除 _resolve_app/_resolve_task_url(:41-51) 与 errcode 重复处理(:84)；删除零使用的 _default_message_builder(:54-63)。注意：所有 5 个调用方都传了 message_builder=build_execute_task_alert（base.py:30-38），改后钉钉消息文案必须与现状一致——先对比两条路径的最终消息文本再动手；tests/test/ 下引用 notifier 已不存在模板属性（error_google_task_templates 等）的死测试一并删除。
3. C6 重试收缩：app/utils/database.py transaction_required(:26-44) 三个 except 分支合一；app/utils/db_retry.py 的 DatabaseRetryManager 类壳(:101-125+:157)降为模块级函数（对外保留 db_retry_manager.execute_with_retry 兼容调用点 repositories/base.py:68 与相关测试，或同步更新它们）。
4. C8 布尔解析：dfcf_api._should_use_proxy_for_kline(:73-77) 与 performance_analysis/portfolio_combiner.py:38 _coerce_bool 改用 app/services/config_manager.py 的 coerce_bool（注意 import 方向：portfolio_combiner 在 services 层可以 import；若分层不允许 utils→services，则在 utils 层落 coerce_bool 并让 config_manager 引用之，二选一并在汇报中说明）。顺手：value_parser._convert_pandas_to_native(:70-89) 缩为 json.loads(df.to_json(orient="records"))（4 个调用方在 performance_analysis/，改后比对输出一致）。

验证：K线取价链路有现有测试（grep tests/ 中 kline/qq/dfcf/akshare/yf）；改后全量 python -m pytest tests/unit tests/integration 全绿。汇报每客户端行数变化。不要 commit。
```

## P10 routes 表驱动化 + 双蓝图去重（约 -320 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中收敛 routes 层（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §D1/D2/D6；建议 P03/P04 先行）：

1. D1 页面路由表驱动化：app/routes/ 下 9 个文件里 34 个"return send_page('x.html')"单行函数（admin.py 85 行、backtest_training.py、backtest_multi_product.py、google_sheet.py、xpl.py 的页面部分、yule.py、global_preview.py、eastmoney_kline.py、auth_pages.py）替换为一张 PAGE_TABLE（path, template, 所需鉴权装饰器）+ add_url_rule 循环（放 app/routes/page_files.py 或新 page_routes.py）。例外必须保留手写：google_sheet.py 的 /create、/detail 版本 dispatcher（缺 version 查任务补参重定向逻辑）；xpl.py 的两个 analyze API 端点。注意保留各蓝图现有 url_prefix 与鉴权语义（login_required 装饰器来源不混用）。改完 grep '/page' 与 url_for 引用确认无端点名依赖（页面链接走路径而非 endpoint）。
2. D2 双蓝图去重：app/routes/backtest_api.py 中 import_excel(73-85)==bmp_import_excel(282-293)、get_task_results_by_task_id(88-115)vs(296-315)、get_global_preview(207-216)vs(375-382) 三对合并——同一视图函数注册到 bt_api_bp 与 bmp_api_bp 两个蓝图（函数体内按 request.blueprint 或 task_type 分发 loader），URL 与响应结构不变。
3. D6：app/routes/xpl.py 的 analyze_data 与 analyze_data_v1（:41-99）抽 _run_analyze(service_fn) helper，两个 URL 与限流配置保持不变（/xpl/v1/analyze 必须保留——xpl_v2 页面在用）。

完成后运行 python -m pytest tests/unit tests/integration 确认全绿；如仓库有页面冒烟类集成测试（test_client get 页面）重点确认 30 个页面路径全部仍 200。不要 commit。
```

## P11 静态 JS：c4/c5/c7 克隆家族 Biz 化（约 -4,500～-5,000 行，前端回归面大）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中完成 F3 半途而废的静态页共享化：c4/c5/c7 的 create+detail 六个文件（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §E1）。这是纯前端 vanilla script 重构，无构建步骤，沿用既有 Biz.* 模式（IIFE 挂 window.Biz，参考 static/js/common/business/form-state.js、task-submit.js、task-polling.js 的写法与模板加载顺序）。

事实基础：google_sheet_c5_create.js(1542) ↔ c7_create(1753) 有 1501 行相同（97%），差异仅 C7 模型版本块（enforceC7PriceMode/validateC7ModelVersionSet）与 c5→c7 字符串改名；c5↔c7 detail 1365/1415 相同（96%）；c4_create 与 c5 相同 57%；四个 detail 文件（含 c3 的 google_sheet_detail.js）共享 ~30 个同名函数。

任务：
1. 在 static/js/common/business/ 新建共享模块（建议 ctask-form.js、ctask-detail.js），以版本配置对象参数化（task_type、localStorage 键、模板任务类型过滤、返回链接、版本专属校验钩子）。
2. 迁移孪生函数：loadWorksheetsForItem、loadGoogleSheetsForItem、applyConfigToForm、bindEvents、addVisualSheetConfigItem/removeLastVisualSheetConfigItem、syncProductCodesToParam1、addProductCodesFromInput、removeProductCode、calculateCombinations、showCombinationPreview、submitTask、getCurrentConfig、normalizeC4Config、loadRestartConfig、loadTemplates、fillFormWithTemplate、cancelTask、startAutoRefresh/stopAutoRefresh、checkTaskStatus、restartTask、groupResults、flattenResults、renderResults、showResultDetail、loadTaskDetail、loadTaskConfig、loadTaskParameters、导出四件套（prepareExcelSaveTarget/saveExcelBlob/downloadBlob/ensureExcelExtension/getExportFilenameFromResponse ×3 份）等。c7 页只留 enforceC7PriceMode/validateC7ModelVersionSet/OHLC 相关差异；c5 留排除年份/K线模式差异；c4、c3 逐步收编。
3. 严格遵守 AGENTS.md 前端红线：普通 script 原位置、禁 defer/module；接口只走 static/js/common/api.js；改字段必须五处同步（表单初始化、localStorage 恢复、模板回填、restart 回填、提交 payload）——本次不改字段，只搬函数，搬完逐页核对这五处行为等价。
4. 每收编一个页面，人工冒烟该页：创建页（填参→提交 payload 对比改前 JSON 一致）、详情页（轮询、重启回填、结果渲染、导出）。

验收：六个文件总行数从 6,921 降到 ~2,400 上下；全部页面功能等价。完成后汇报每文件行数变化与共享模块清单。不要 commit。（若担心回归面，可拆成 c5+c7 create、c5+c7 detail、c4、c3 四个子批执行。）
```

## P12 静态 JS：backtest 两家族参数化（约 -1,900～-2,100 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中把 backtest_training 与 backtest_multi_product 的三对克隆页参数化（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §E2；模式参考 P11 的 Biz.* 做法）：

事实基础：detail 1133/1239 相同（91%，40+ 同名函数：groupSummaryRows、buildPaginationPages、applyC3MetricSelection、formatC3MetricValue、getC3MetricCellClass、getDefaultC3MetricKeys、loadC3Summary、inferModelVersion、handleRestartFromScratch/ResumeRestartTask/StopTask、format*Value 家族）；result 847/897 相同（94%：renderCharts、renderSharpeCompare、renderProfitMonthly、renderMonthlyExcessReturnsChart、renderExcessMetricsChart、renderRepairDaysChart、buildBarChart、buildLineChart、exportV1Details、exportWordReport、copyRawJson、loadBacktestResult、showLoading/hideLoading/destroyChart）；list 427/467 相同（91%）。create 仅 21% 相同——不动 create。

任务：在 static/js/common/business/ 新建共享模块（backtest-detail.js、backtest-result.js、backtest-list.js），以 product-mode 配置（single|multi）参数化；multi 侧增量仅 inferProductModelVersion、copyAllTaskParameters、renderTabDailyReturns 三处，作为模式钩子。两个 result.html 模板已加载 common/business/result-table.js(698)，共享层与其配合而非重复。

验收：detail/result/list 六文件总行数显著下降（约 -1,900～-2,100）；两族页面功能等价（列表筛选/分页、详情汇总与 C3 指标选择、结果图表与 Word/Excel 导出）。全量 python -m pytest tests/unit tests/integration 全绿（防页面路由回归）。不要 commit。
```

## P13 静态 JS：公共件收敛 + Biz 死函数删除（约 -250 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中收敛静态 JS 公共工具（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §E3/E5/E8；建议 P11/P12 之后执行以减少重复改动）：

1. E3：static/js/common/utils.js 增加逃逸/格式化标准件，删除各页私设副本——escapeHtml 全仓 24 份定义（template-auth.js、common/business/task-list.js:40、common/business/result-table.js + static/js/pages/ 21 个文件，各 ~7 行）、formatTime ×4（template-auth.js:172、task-list.js:49、admin_logs.js、admin_config.js）、debounce ×4（template-auth.js:240 + c4/c5/c7_create 各一，P11 完成后可能只剩一份）、showAlert ×4（result-table.js、xpl_index.js、xpl_v1 已删则忽略、xpl_v2.js）。注意：改前确认每个目标模板已加载 common/utils.js（grep templates/ 下 script 引用），未加载的补一行 script 标签。
2. getStatusText 两份文案已分叉（template-auth.js:183 running:"执行中"/error:"执行出错" vs business/task-list.js:68 running:"运行中"/error:"失败"）——合并前先向用户确认采用哪套文案（或按上下文保留两套命名不合并）。
3. E5 死函数删除（grep 复核零调用后删）：common/business/task-submit.js 的 clearAllParameters(:18-29)、handleTaskEvent(:87-96)（saveAsTemplate/submitTemplate/generateCombinations 存活勿动）；common/business/task-polling.js 的 manualRefresh(:24-42)（同时评估把 google_sheet_detail.js:74 的私有副本切到共享实现）；common/utils.js 的 sanitizeJSONString(:6-12)。
4. E8 顺手项：6 个详情页的 startAutoRefresh ×6、formatDuration ×4、getProgressBarClass ×4、checkTaskStatus ×4 统一切到 common/business/task-polling.js（c3 与 backtest 详情页模板需补加载该文件）；eastmoney_kline_index.js:31-40 与 xpl_index.js:31-40 的手写 formatNumber/formatPercent 并入 common/utils.js。

验收：所有涉及页面冒烟（列表、详情轮询、导出、通知浮层）；全量 python -m pytest tests/unit tests/integration 全绿。不要 commit。
```

## P14 template-auth.js 收敛 + admin 域迁移（约 -90 行 + 单一 fetch 链路）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中统一静态页 fetch 链路并清理 template-auth 死重（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §E4/E6）：

1. E4 双栈合一：static/js/common/api.js 的 endpoints 表增加 admin 域（users/roles/logs/scheduler/dashboard/tasks 管理端点），然后迁移 13 个绕过 api.js 的页面文件（手写 URL 共 54 处，大户：admin_tasks.js 13、admin_users.js 7、admin_scheduler.js 7、admin_roles.js 5、admin_logs.js 5、xpl_v2.js 3、admin_config.js 3，全量清单用 grep -rnoE "['\"\`]/(api|admin/api|xpl)[^'\"\`]*" static/js/pages 提取）。迁完后删除 template-auth.js 的 requestJson/ajaxRequest（:322-390 中相应部分，约 35 行）与相关全局暴露。JWT 401 自动 refresh 逻辑保留在 template-auth（api.js:3-4 的文档化分工不动）。
2. E6 死重清理：isAuthEnabled() 硬编码 true（:94-98），删除其 3 个死分支（:702-705、:755-760、:216-218 admin mock 支路）并删掉该函数或保留为常量；删除空 Set templateUnsupportedPaths(:27) 及喂它的过滤；legacyPathMap(:16-25) 与 app/startup.py:616-629 服务端归一表重复——若确认服务端归一已覆盖全部入口则删除客户端映射（不确定则保留并在汇报中说明风险）。
3. 迁移逐页冒烟 admin 页面（登录、列表、增删改、日志滚动、调度器操作）。

全量 python -m pytest tests/unit tests/integration 全绿（防路由回归）。不要 commit。
```

## P15 静态 JS：c31/c3 create 共享核心（约 -400～-500 行）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中让 c31 与 c3 的创建页共享核心（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §E7；在 P11 的共享模块与版本配置模式确立后执行）：

事实基础：google_sheet_c31_create.js(1897) 与 google_sheet_create.js(1297) 共享 659 行；token 选择器块 ~200 行逐字（syncSelectedTokenMeta、applyPendingTokenSelection、renderGoogleSheetTokenOptions、loadGoogleSheetTokens、importGoogleSheetToken 等）；另有 loadTemplates、fillFormWithTemplate、saveFormData、loadSavedFormData、clearSavedFormData、saveAsTemplate、getCurrentConfig、submitTemplate、handleTaskEvent、loadFromTemplate、fillFormWithRestartTask 同名孪生（部分已在 Biz.taskSubmit，页面仍是私有拷贝——注意与 P13 的死函数清单衔接，页面私有拷贝在迁移到 Biz 后删除）。c31 真正独有：多 Sheet 批量 UI 与 applyRecentYears/日期区间模式（约 40%），保持页内不动。

任务：把上述共享函数迁入 common/business/（token-selector.js 新模块 + 复用既有 form-state/task-submit），两页以配置差异（task_type、是否批量模式）接入；c31 的 localStorage 恢复与 C31 三层透传字段（前端 config → child_config → 执行 service）行为严禁变化。

验收：两页创建/模板/重启回填/提交 payload 与改前 JSON 一致；全量 python -m pytest tests/unit tests/integration 全绿。不要 commit。
```

## P16 决策清点（不执行，等裁决）

```
以下事项需要产品决策，逐条向用户确认后再动（背景：docs/design/ponytail-audit-2026-09/02-决策与执行计划.md §3）：
A1 双前端取舍（0～-14,700 行）；  不处理，templates 为当前前端，frontend为未来前端
A3 yule 页去留（-1,300）； 删除
D3 result_api 响应形状二选一（需迁移 google_sheet_c4_detail.js:669 或 task-polling.js:90 之一）； 择优
D4 reconcile 端点留痕或删；  留痕
D5 旧书签兼容蓝图去留； 删除
D8 /api/meta/versions 并入 enums； 执行
B18 stock_meta 双写路径归一（app/services/stock_search_service.py:150 vs stock_metadata_service，需定归一语义）；可以，择优
E9 yule 五动画引擎合一；移除
E10 naive-ui 单视图依赖（移植 frontend/src/views/task/TaskListInner.vue 后弃用）； 移除
C5 前端是否暴露半年跨度/随机价格选项。暴露
```

## P17 卫生批次（约 -250 行 + 1 个 bug 修复）

```
在仓库 C:\Users\fuqing\Desktop\google_sheet_task 中执行卫生批次（背景：docs/design/ponytail-audit-2026-09/01-审计报告.md §F1/F2/F3）：

1. F3 范围外 bug（先修）：app/services/export_service.py:359 使用了未定义的 logger（_stream_stock_zip 异常路径会 NameError 掩盖原始错误），按该文件头部既有 get_logger 用法补 logger 定义。
2. F1 tests 杂物：tests/test/test_value_parser.py 是唯一被 pytest 真实收集的测试且住在杂物抽屉，迁到 tests/unit/（若与 tests/unit/test_value_parser_and_task_types.py 内容重叠则合并后去重）；其余杂物（tests/T_word.py——import 不存在的 d 模块、tests/计算指数.py、tests/test/ 下的 xpl_service copy.py/te.py/A.py/xpl.html/.cache.txt 及散脚本）迁入 tests/archive/（pytest.ini 已 norecursedirs 排除，确认迁移后全量收集不变）。
3. F1 scripts 归档：scripts/backfill_metrics_payload.py（243 行，2026-08 指标载荷一次性修复）向用户确认生产是否还需回放；不需要则移入 tests/archive/ 或加 README 标注归档。
4. F2 文档修正：AGENTS.md 中"run.py/startup.py 里还有运行时 ALTER TABLE 补列的 schema 修补逻辑"表述已过时——run.py 仅 27 行干净入口，schema 修补实际只在 app/startup.py:43-360 一处；同步修正 docs/ 内若有的相同表述（grep ALTER TABLE 确认）。

全量 python -m pytest tests/unit tests/integration 全绿（确认收集路径未变）。不要 commit。
```
