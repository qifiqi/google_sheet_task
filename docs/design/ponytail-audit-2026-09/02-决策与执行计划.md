# 决策与执行计划（2026-09-08）

> 上游文档：[01-审计报告.md](01-审计报告.md)（发现编号 A1…F3 均出自该文）。本文件记录用户裁决、执行批次与状态跟踪。提示词见 [03-执行提示词.md](03-执行提示词.md)。
> 执行状态：2026-09-08～09 已按 02 §2 推荐顺序完成 P01/P02/P03/P04(+yule)/P17/P06/P05/P05b/P07/P10，及 P08 的 B6+B8、P09 的 C6+C8+B18、P13 的死函数与 escapeHtml×21 收敛，全部通过回归门禁（基线 13 存量失败，零新增），详见 §4；剩余子项按 03 文档提示词逐会话执行。

## 1. 用户裁决（2026-09-08）

### 1.1 B1（C5/C7 `_get_all_parameters` 合一）—— 执行，且 C5 同步新能力
- 裁决原文："这个c5同步半年和随机价格把"。
- 执行口径：C5/C7 的 `_get_all_parameters` 合一为共享实现；共享实现支持**半年跨度**与**随机价格**两条 C7 增量路径；**C5 接入同一实现并具备这两项能力**（C7 行为不变；C5 在配置未提供相关键时与现状一致，属"能力就位、按需启用"）。
- 边界约束（AGENTS.md）：custom K线分支不进 `_get_all_parameters`（B16 的 `_get_custom_kline_data/_get_custom_parameters` 合并走独立共享模块，见 P05 附带项）；去重/断点归基类，子类不得本地覆盖。
- 前端是否给 C5 暴露随机价格/半年跨度选项：不在本次范围，作为后续项登记（见 §3 未决）。

### 1.2 A2（xpl v1/v2 二选一）—— 保留 v2，删 v1 页面链
- 删除：`/xpl/v1` 页面路由、`templates/xpl/v1.html`、`static/js/pages/xpl_v1.js`（含 `static/css/pages/xpl_v1.css` 若存在）、v1 页面测试。
- 同步修改：`app/navigation.py` 导航项（`/xpl/v1` → `/xpl/v2`，标签去掉 V1 字样）、`xpl_index`/相关页面对 v1 的入口链接。
- **必须保留**：`/xpl/v1/analyze` 与 `/xpl/v2/analyze` 分析 API（v2 页面调用的是 v1 API，纯历史命名）；`xpl_index` 与 `/xpl` 主页不动。
- SPA（frontend/src）内的 xpl/v1 Vue 视图不属于本条范围（随 A1 双前端决策处理）。

### 1.3 A4（stock_sdk）—— 全保留
- 不做任何删减。01 报告 §A4 的发现仅留档。

### 1.4 其余条目
- 用户指示："其余直接执行、本地保留文档先、然后给我提示词"。本文档与 01/03 即"文档先"的落地；**所有可执行条目已编排为 P01～P17 提示词**（03 文档），待用户逐条触发执行。
- 未决决策项（A1/A3/D3/D4/D5/D8/B18/E9/E10）不编入执行提示词，先留档等裁决（P16 为决策清点项）。

### 1.5 第二轮裁决（2026-09-08，P16 清点）

- **A1 双前端：双轨并存。** 静态页与 Vue SPA 均为活产品：P11～P15 静态页共享化照常执行；Vue SPA 照常独立开发。E10（naive-ui 单视图依赖）不在本计划执行范围，随 Vue 工程自身迭代处理。
- **A3 yule：整体删除。** 删 `app/routes/yule.py`（含 `__init__.py` 注册）、`templates/yule/`（2 文件）、`static/js/pages/yule_sjxz.js`、`static/js/pages/yule_index.js`；E9（五动画引擎合一）随之关闭。并入 P04 批次执行。
- **C5 前端暴露：本批一并交付。** P05 完成 C5 服务端同步半年跨度+随机价格后，`google_sheet_c5_create.js` 同批增加对应控件（price_mode=随机价格、半年跨度），交付顺序在 P11 之前。
- **D3 result_api：统一分页形状。** `/api/tasks/<id>/results` 固定返回 `{items,total,pages,current_page,per_page}`；迁移 `google_sheet_c4_detail.js:669` 的不分页调用。并入 P10 批次执行。
- **默认处理项（无异议按此执行）**：D4 reconcile 端点保留并补部署运维文档；D5 旧书签兼容蓝图保留（12 行保险）；D8 `/api/meta/versions` 暂保留（随 A1 双轨并存继续存在）；B18 stock_meta 双写归一到 `stock_metadata_service`（P09 顺带）。

## 2. 执行批次与依赖

| 批次 | 内容（01 报告条目） | 风险 | 依赖 |
|---|---|---|---|
| P01 | utils 死代码快删（C2/C3/C5/C7） | 低 | 无 |
| P02 | services 死代码快删（B10/B11/B12/B13/B14） | 低 | 无 |
| P03 | xpl 保留 v2 删 v1 链（A2 裁决） | 低 | 无 |
| P04 | routes 死接口群（A5/D7） | 低 | 无 |
| P05 | C5/C7 `_get_all_parameters` 合一 + C5 同步半年/随机价格（B1，附带 B16） | **高**（核心任务链路） | 建议先做 P06 铺垫（result_payload/check 骨架已共享的部分先稳住） |
| P06 | services 小合并（B4/B5/B9/B15/B17/B19） | 低-中 | 无 |
| P07 | 轮询骨架上提 base（B3） | **高** | P05 之后做，避免同区冲突 |
| P08 | 指标投影与 helper 归并（B2/B6/B7/B8） | 中 | 无 |
| P09 | utils 五客户端共享件 + 通知/重试收缩（C1/C4/C6/C8） | 中 | P01 先行（先删死重量再抽共享件） |
| P10 | routes 表驱动化 + 双蓝图去重（D1/D2/D6） | 中 | P03/P04 先行（xpl/死接口先定形） |
| P11 | 静态 JS c4/c5/c7 克隆家族 Biz 化（E1） | 高（纯前端回归面） | 无 |
| P12 | backtest 两家族参数化（E2） | 高 | 无 |
| P13 | JS 公共件收敛 + Biz 死函数（E3/E5/E8） | 中 | P11/P12 之后做可少改一半文件 |
| P14 | template-auth 收敛 + admin 域迁移（E4/E6） | 中 | 无 |
| P15 | c31/c3 create 共享核心（E7） | 中 | P11 模式确立后 |
| P16 | 决策清点（A1/A3/D3/D4/D5/D8/B18/E9/E10） | — | 等用户裁决 |
| P17 | 卫生（F1/F2/F3） | 低 | 无 |

推荐执行顺序：P01 → P02 → P03 → P04 → P17 → P06 → P05 → P07 → P08 → P09 → P10 → P13 → P14 → P15 → P11 → P12（P11/P12 放最后：体量最大、回归面在前端）。

## 3. 决策清单（2026-09-08 二轮后全部闭环）

| 编号 | 问题 | 裁决 |
|---|---|---|
| A1 | 双前端取舍 | **双轨并存**：静态页与 Vue SPA 均保留，P11～P15 照常执行 |
| A3 | yule 娱乐页去留 | **整体删除**（并入 P04） |
| D3 | result_api 双响应形状 | **统一分页形状**（并入 P10） |
| D4 | reconcile 维护端点 | 保留 + 补部署运维文档 |
| D5 | 旧书签兼容蓝图 | 保留（12 行保险） |
| D8 | /api/meta/versions | 暂保留（双轨并存下仍有存在价值） |
| B18 | stock_meta 双写路径 | 归一到 stock_metadata_service（P09 顺带） |
| E9 | yule 五动画引擎 | 随 A3 删除，关闭 |
| E10 | naive-ui 单视图依赖 | 随 Vue 工程自身迭代，不在本计划范围 |
| C5 前端暴露 | 半年跨度/随机价格控件 | **本批一并暴露**（随 P05 交付） |

## 4. 状态跟踪（2026-09-08 执行日终更新）

> 本轮会话已执行批次均通过全量回归门禁（`python -m pytest tests/unit tests/integration`）。
> **基线**：13 个存量失败（`test_kline_service`×6、`test_c_series_services`×1、`test_c7_model_version`×1、`test_backtest_services`×1、`test_kline_adjustment`×1、`test_kline_sheet_guardrails`×2、`test_strategy_backtest_report_charts`×1、`test_value_parser_and_task_types::test_parse_date_supports_iso_shapes`×1——均已验证在改动前的干净树上同样失败，其中后者断言 `2026/08/29` 应解析为 None 而 dateutil 实际可解析，属测试与实现的历史分歧）。
> 已知网络依赖用例：`test_c7_random_price_builds_requested_high_low_groups` 依赖内网 K 线库连通性，超时即失败（干净树同样复现）。

| 条目 | 处置 | 状态 | 备注 |
|---|---|---|---|
| P01 utils 死代码快删（C2/C3/C5/C7） | 已执行 | ✅ 完成 | 另删除 test_c7_model_version 中 2 处对已迁移符号的 patch 目标修正 |
| P02 services 死代码快删（B10-B14） | 已执行 | ✅ 完成 | 连带删除仅存于死包装上的单测 `test_normalized_weights_*`；`performance_analysis_service.py`、`model_summary/facade.py` 已删除 |
| P03 xpl 保留 v2 删 v1（A2 裁决） | 已执行 | ✅ 完成 | 导航 key 保留 `xpl_v1`（startup 只增改不删，改 key 会在存量库残留死链），path→/xpl/v2、label→回测数据分析；`/xpl/v1/analyze` API 保留 |
| P04 routes 死接口群（A5/D7）+ yule（A3 裁决） | 已执行 | ✅ 完成 | 删 database_api/yule 蓝图、admin_api 两端点、system-logs 端点+`query_task_system_logs`、scheduler_service 三个随之死亡的方法、SPA 死封装 5 处；yule 全链（route/模板/JS/CSS）删除 |
| P17 卫生（F1/F2/F3） | 已执行 | ✅ 完成 | export_service logger 缺失已修（F3 范围外 bug）；tests 根杂物与 `tests/test/` 抽屉迁 `tests/archive/legacy_drawer/`（`test_value_parser.py` 的 datetime 用例断言并入 unit 测试）；AGENTS.md 过时表述已修；`scripts/backfill_metrics_payload.py` 待确认后归档 |
| P06 services 小合并（B4/B5/B9/B15/B17/B19） | 已执行 | ✅ 完成 | 另核实 c3/c5/c7 的 `self.YF_api` 并非死代码（传入 KlineService），未动 |
| P05 C5/C7 `_get_all_parameters` 合一 + C5 同步（B1，含 B16） | 已执行 | ✅ 完成 | 骨架上提为 `base._expand_auto_year_parameters` + `base._expand_random_price_groups`；C5 同步近半年(0.5)区间与 random_price 能力（base 默认钩子透传 `random_price_range/random_group_count`）；C7 行为逐字段不变；custom K线仍留各 service |
| P05b C5 创建页暴露控件（二轮裁决） | 已执行 | ✅ 完成 | c5 模板+JS 五处同步（表单初始化/localStorage/模板回填/restart 回填/提交载荷）；`getExcludedRecentYears` parseInt→parseFloat（0.5 必须） |
| P07 轮询骨架上提 base（B3） | 已执行 | ✅ 完成 | `base._poll_google_sheet_completion`（刷新时刻表/延时/取消/超时/后置钩子统一）；c5/c7/c4/training 四处体移入 `_attempt` 闭包，写回/校验/提取闭包逐字保留；set_googl_val/_validate_check_values 的深度参数化登记为后续项（见 P07 提示词） |
| P10 routes 表驱动化 + 双蓝图去重 + D3 | 已执行 | ✅ 完成 | `page_files.register_page_routes`（9 文件 34 视图收敛；auth_pages 保留手写——`url_for('auth_pages.login_page')` 是唯一活端点引用）；backtest_api 三对端点一视图双蓝图（`_parse_json` 容错语义统一，bt 侧 json.loads 严格语义被合并）；D3 统一分页形状信封（全量模式以单页承载，CSV 导出不受影响）；D6 xpl `_run_analyze` |
| P13 死函数删除（E5 部分） | 已执行 | ✅ 完成 | 删 Biz.taskSubmit.clearAllParameters/handleTaskEvent、Biz.taskPolling.manualRefresh、utils.sanitizeJSONString |
| P13 escapeHtml 收敛（E3 部分） | 已执行 | ✅ 完成 | utils.js 新增空安全 escapeHtml；21 个页面私设定义全部删除（21 个模板均已验证先于页面脚本加载 utils.js）；template-auth/task-list/result-table 三份因加载顺序敏感性保留 |
| P14 E6 部分（template-auth 死分支） | 已执行 | ✅ 完成 | 恒真 `isAuthEnabled()` 及其 3 个死分支删除（isAdmin 化简为布尔返回）；空 `templateUnsupportedPaths` 集合及死过滤删除；`legacyPathMap` 保留（与 startup 归一表的职责边界需运维确认后再删） |
| P08 指标投影与 helper 归并（部分：B6/B8） | 已执行 | ✅ 完成 | 新增 `app/utils/formatting.py`（normalize_scientific_text/max_yearly_repair_days/strip_html_tags/parse_lenient_json）；`_all_entry`×5 收敛为 `historical_metrics.find_all_entry`；`_derive_year_max_excess_drawdown`×2 合一（**注意**：两份符号约定相反，共享实现以 direction 参数显式表达，multi=+1、report_query=-1） |
| P08 B2/B7（指标投影×5、解析×7） | 部分执行 | ✅ golden 守卫已建 / ⏸ 全量归并暂缓 | 新增 `tests/unit/test_summary_projection_golden.py`（5 个快照测试锁定五入口当前输出，含 report_query 卡玛 model_value 为空等存量怪癖）；`historical_metrics.collect_summary_all_entries` 收敛 multi/report_query/extractor 三处 all-entry 查找管道。执行发现：五处投影容差语义有实质差异（multi 键回退+None 容差 / result_mapper 0 容差 / exporter '--' 占位；avg 月超额分母定义不同），完整数值契约归并需先评审这些差异，盲全会改变导出数值；B7 七个解析变体同理（千分位/符号循环/恒除 100 等各自容差不同）——需各自的无效输入 golden 用例后再归并 |
| P09 C6+C8+B18 | 已执行 | ✅ 完成 | DatabaseRetryManager 类壳拆除（保留 `db_retry_manager` 兼容命名空间，commit_with_retry 语义不变、test_a1_bugfixes 全绿）；value_parser 新增 coerce_bool 单一实现（config_manager 保留 _MISSING 哨兵壳，dfcf/portfolio_combiner 改用，后者 `_coerce_bool` 为零调用死代码直接删）；stock_meta 双写归一到 normalize_stock_payload（保留"无 market_type 不写入"过滤，股票代码自此归一为标准格式） |
| P09 C1（五客户端共享层，部分） | 部分执行 | ✅ 复权映射/fetcher 合并完成 | `kline_adjustment.sina_adjust()` 收敛 qq/akshare 两份逐字 `ADJUST_TYPE_MAP`（保留各自 default 语义：qq 未知值→qfq，akshare 未知值→不复权）；akshare `_fetch_sina_daily`/`_fetch_hk` 合并（同一套行映射）。暂缓：`build_stock_session`/`request_with_retry`/`build_kline_row` 三件套——四个数据源量纲换算（A股 ×100/×10000、美股不换算、yahoo 收盘×量估额）与重试节奏各不相同，与 B2 同类需逐源 golden；session 构造参数差异大于收益 |
| P09 C4（alert_decorator，部分） | 部分执行 | ✅ 死默认构造器已删 | `_default_message_builder` 删除、`message_builder` 改为必填（5 个调用点全显式传 `build_execute_task_alert`）。暂缓：`send_failure_alert` 整体委托 `send_task_notification`——notifier URL 链优先 DING_TALK_DETAIL_BASE_URL/PUBLIC_BASE_URL（decorator 现走 BASE_URL），委托会改变线上消息详情链接来源；`detail_url` 的"重复"实为消费端对返回值的解释 |
| P13 剩余（E3：formatTime/debounce/getStatusText、E8 轮询样板） | 待执行 | 未开始 | getStatusText 两份文案分叉需先定文案；其余见提示词 P13 |
| P14 template-auth 收敛 + admin 域迁移（E4/E6） | 待执行 | 未开始 | 提示词 P14 |
| P15 c31/c3 create 共享核心（E7） | 待执行 | 未开始 | 提示词 P15 |
| P11 子批1：c5+c7 create 收敛（E1 首批） | 已执行 | ✅ 完成（2026-09-09） | `google_sheet_c7_create.js` 改造为共享控制器 `Biz.ctaskCreate.init(config)`（自举自 `window.__CTASK_CONFIG`）；`google_sheet_c5_create.js` 删除（**-1,542 行**，净 -1,525）；c5/c7 模板内联版本配置（versionQuery/taskType/templateTaskType/klineDataSourceDefault/countRandomGroupsMultiplier/modelVersion）。**页面冒烟验证**：本地冒烟服务（run_smoke.py，5001 端口，无 bootstrap）+ 浏览器 fetch 拦截，c5/c7 各 4 场景（auto/custom/random_price/n_plus_1+排除半年）提交 payload 与重构前基线逐字段 MATCH（name 时间戳字段除外）；全量门禁 13 存量失败零新增。文件名 `google_sheet_c7_create.js` 暂保留（c5 页加载 c7 命名文件为过渡态，最终改名为 ctask-create.js 是纯 git mv + 两处模板引用） |
| P11 剩余子批（c5/c7 detail 对、c4 create/detail、c3 detail） | 待执行 | 未开始 | 复用 P11 子批1 的冒烟方法（run_smoke.py + fetch 拦截 + 场景差分）；提示词 P11 |
| P12 backtest 两家族参数化（E2） | 待执行 | 未开始 | 提示词 P12 |
| P16 决策清点 | 已闭环 | ✅ | 二轮裁决全部落定（§1.5、§3） |
| A4 stock_sdk | 保留 | ✅ 关闭 | — |

## 5. 验证口径（每个批次通用）

- 回归：`python -m pytest tests/unit tests/integration`（统一入口；tests 根目录散件不纳入）。
- 不主动 commit（除非用户当次指示）；改动留在工作区供 review。
- 遵守 AGENTS.md 分层与既有约定（统一信封、facade 入口、base 钩子、custom K线边界、JS 分层与回填五处同步）。
- 删除类改动完成后 grep 复核引用归零；行为类改动需给出对应测试或补测试。
