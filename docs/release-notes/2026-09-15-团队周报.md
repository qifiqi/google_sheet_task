# 团队周报：dev_vue（2026-09-14 → 2026-09-15）

> 作者：fuqing · 分支：dev_vue · 窗口内共 26 个提交 + 本报告落盘提交

## 口径与证据

- **时间窗口**：严格"本周" = 2026-09-14（周一）00:00 → 2026-09-15，东八区。
- **PR 核实（gh 已认证）**：仓库历史上共 1 个 PR（#1 "Dev vue"，状态 OPEN，未合并）。即本周**没有任何已合并 PR**，全部工作以直接提交方式落在 dev_vue；`origin/main` 最后提交停在 2026-06-24，本周无内容合入 main。
- **分叉说明**：窗口内本地与 origin/dev_vue 曾短暂分叉（本地独有 1 个提交、远端独有 6 个，远端线为 09-15 下午推送），落盘前已通过 rebase 拉平为同一线，原本地提交 `52a6fef` 重写为 `bafd33d`。
- **未合入的并行线**：`1e9f07a`（refactor(core): 数据层与鉴权迁移至远程 HTTP 后端，db-to-http）在 `dev_db_to_http` 分支，本周有进展但未合入 dev_vue。

## 本周主线

**Word 报告多指数（多基准）改造**：从设计定稿到引擎、图表、权重表、导出缓存全链路落地。设计文档见 `docs/design/multi-index-word-report/README.md`（`fc5975f`，239 行）。

## 功能（12 个）

| 提交 | 内容 | 规模 |
|---|---|---|
| `63c953c` | **Word 报告多基准组装**——每个指数各跑一次引擎并展开宽表（本周核心改造） | +566/−301 |
| `ee36635` | 恢复暂存的报告成交量/权重相关性与导出多选弹窗 | +646/−57 |
| `1d3fb4b` | Word 报告图表多基准序列化 | +300/−122 |
| `d85069d` | 基准指数支持按比例缩放（语义 A：每指数各自带比例） | +365/−113 |
| `16380bd` | 指数基准支持比例缩放与权重表双轨展示 | +118/−61 |
| `725520f` | Word 导出基准指数字段列表化并加固校验 | +217/−14 |
| `bc064cd` | 权重分配表新增「ETF资产总数」列，按亿/万缩写展示 | +110/−19 |
| `60dc1c2` | 权重表成交量缩写并预取平均成交额 | +169/−73 |
| `1f8f86d` | Word 权重表改版与组合指数开关 | +405/−157 |
| `84a4677` | ETF 资产总数回退总市值，支持来源标记与缓存 | +146/−16 |
| `488d673` | Word 报告导出磁盘缓存 + 管理端清理接口（本周最大单提交） | +734/−4 |
| `2796597` | 市场类型与数据源联动逻辑 | +61/−7 |

## 修复（8 个）

| 提交 | 内容 |
|---|---|
| `390510a` | 修正超额收益计算与图表绘制 |
| `89907b7` | 移除 RPT-S 报告校验中未声明的 `index_return_id` 引用 |
| `3bcfec2` | 风险调整章节超额行补齐基准占位列 |
| `ab47387` | 超额列头与"全部基准"标记统一为「同股只展示比例」规则 |
| `112a488` | 补齐 ETF 资产总数取数入口 |
| `676dac7` | 统一沪市代码为 SH 后缀（涉及 10 个文件） |
| `26846f2` | 全局预览改按股票导出 ZIP |
| `4a1df1c` | 列表标识自适应执行参数 |

## 体验（2 个）

说明：这两条原始 type 前缀均为 `fix`，按内容（图表呈现/交互优化）归入体验类，此为分类判断而非仓库事实。

- `3efcebb` 多基准报告图表图例布局优化（+56/−11）
- `a99ecf1` 相关系数热力图坐标原点移至左下角（符合常规阅读习惯）

## 工程改进（4 个）

- `fc5975f` 新增多指数 Word 报告改造方案设计文档（docs/design/multi-index-word-report/）
- `16efef1` 移除未使用的方法、优化代码结构（9 文件，净 −91 行）
- `d22e93d` 新增 ETF 资产总数测试用例（+127）
- `bafd33d` AGENTS.md 增补"大规模修改后需提供修改文档"约定

## 状态与风险

1. `dev_db_to_http` 分支的 db-to-http 重构（`1e9f07a`）未合入 dev_vue，后续合并需关注冲突面。
2. 本周合并入 main 前未运行全量回归（`python -m pytest tests/unit tests/integration`），建议合并后补跑。
3. GitHub 网络链路不稳（fetch 多次被重置），推送与同步操作需留意重试。
